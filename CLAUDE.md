# LogLSM — Контекст проекта для Claude

> Актуально на: **20 июля 2026**
> При старте сессии — прочитать также:
> - `actual/session_notes_2026-07-20_activation_rollback_handoff.md` — **ЧИТАТЬ
>   ПЕРВЫМ (20.07, САМОЕ СВЕЖЕЕ, точка продолжения).** Рабочее состояние
>   **A=26.07.19 18:58, W=26.07.19 20:08** (детектор полок работает). Активация
>   Шаг 1/2 была реализована и ПОЛНОСТЬЮ ОТКАЧЕНА (ломала «Данные»). ⚠ Кнопка ▶
>   «полная активация» ДЕСТРУКТИВНА (стирает чип NOR → потеря данных) — не
>   запускать на приборе с данными. План продолжения активации — в заметке.
> - `actual/session_notes_2026-07-18_watchdog_and_memtest.md` — **(17–18.07).** (1) Сборка/прошивка в VS Code — регламент
>   РЕШЁН (cleanRebuild+preLaunchTask, mtime-баг разобран). (2) **Вибрация v2
>   ✅ ЗАВЕРШЕНА** — vib1/vib2 пишутся, подтверждено на железе (код был верен,
>   вся эпопея была в сборке). (3) Тест памяти доведён (авто-обновление панели,
>   термотест по блокам, «Ошибок, байт»). (4) **Маркер достоверности часов**
>   (BKP_DR0) реализован. (5) **Сторожа IWDG+RTC (design B) ✅** — 32 c IWDG +
>   RTC-глажение (Проверка 1 c / Пассив 25 c), тест 0x26, сброс счётчиков 0x24,
>   индикация в W (моргание/шапка «● сторож сброс»). Шкала активации =
>   кликабельные сегменты-кнопки. Версионирование A/W — правило в Firmware-
>   разделе. **КАЛИБРОВКА СКОРОСТИ ✅** (§10: kSpeedCal[9] в Data.c, −3%→±1%,
>   клип гироскопа ~333 rpm = предел A; защита наработки от перевода часов в
>   цикле). ⌚ синхро по границе секунды. **ЧАСЫ ОТКАЛИБРОВАНЫ** (19.07: покой
>   +305 ppm → RTC smooth-calib CALM=320, rtc.c; контроль — ночной тест 1б,
>   `clock_drift_test_plan.md`). **КАЛИБРОВКА СКОРОСТИ ЗАКОНЧЕНА** (узлы 50/70
>   → 1.05/1.045, чисто гироскопные). Баг «нет ответа» 1-го цикла ИСПРАВЛЕН
>   (очередь сирот пушей в W); 1-й цикл холодного старта −2…4 c = принятая
>   характеристика. **КОНТРОЛЬ: 30/30 циклов с интервалами 0 c** —
>   измерительная часть ГОТОВА. ТЕКУЩЕЕ (§11): шкала активации — сегменты
>   1-3 работают (Стереть/Сброс WDT/Синхро время), «Сервис» в правом углу
>   менюбара; СЛЕДУЮЩИЙ ШАГ = запись в контроллер (iflash стр.123): 0x27/0x28,
>   ts_activation в GET_STATS, сегмент 4 «Активация» (план в §11 заметки).
>   Версии: **A=26.07.19 14:30, W=26.07.19 14:01**
>   (§9 заметки — промежуточный финал
>   дня: вибро-тракт закрыт [авторейндж ±2→16g, плотная выборка ~31/с, удар до
>   25 g], живые «Данные» по пушу 22 Б, столбчатые графики+ползунок, формат
>   готов под B [канал «удары» скрыт до WHO_AM_I 0x70]). **ВИБРАЦИЯ ФИНАЛ**
>   (§10-11): «уровень»=робастная медиана |dev| (median-трекер, удароустойчива),
>   «пики»=vibPeak фикс 0..16g; живая валидация Мин/Макс на «Стенде»; ползунок
>   истории; «Сервис» в правом углу менюбара; сегмент 3 активации «Синхро время».
>   **СТЕНД TODO (§12)**: вернуть строку «Циклов» (кол-во раб. циклов) +
>   «Интервалов»=смены скорости внутри цикла; многоскоростной цикл → фикс МАКС
>   (max_rpm уже в пуше). **СДЕЛАНО 19.07** (W=26.07.19 18:45): поле «Циклов»
>   (группа раб. циклов, повтор до Стоп), разметка (Скорость шапкой, заголовки
>   вправо), «Скор.макс» vs max_rpm. **СЛЕД. ЗАДАЧА (§12, прошивка): детектор
>   ПОЛОК скорости** — регистратор сегментирует непрерывное вращение по полкам,
>   на каждой фиксирует МАКС, отдаёт N подскоростей → в тесте раскладываем по N
>   строкам (задано→измерено). **✅ РЕАЛИЗОВАНО 19.07** (A=26.07.19 18:58,
>   W=26.07.19 19:31): детектор полок в Data.c (полоса ±15%/±4, дебаунс 3, max
>   по полке), суб-пуш 0x29 только в сервис (Flash не тронут), многострочный
>   вывод «задано→измерено», макс помечен «◄ макс»; Только+/− = ровная лестница
>   Мин..Макс. На железе: 10/155/300→0% на макс. Версии: **A=26.07.19 18:58,
>   W=26.07.19 20:08** (полки: сопоставление по ЗНАЧЕНИЮ [не позиции — иначе
>   +540% при слиянии близких], цифры по правому краю колонок, макс = зелёная
>   строка с «◄» через QSyntaxHighlighter, слово «макс» убрано; Только+/− ровная
>   лестница; дефолт простого прогона Циклов/интервалов 1/1, скорость 10-300)
>   (§9 заметки — промежуточный финал
>   дня: вибро-тракт закрыт [авторейндж ±2→16g, плотная выборка ~31/с, удар до
>   25 g], живые «Данные» по пушу 22 Б, столбчатые графики+ползунок, формат
>   готов под B [канал «удары» скрыт до WHO_AM_I 0x70]).
> - `actual/session_notes_2026-07-13_v2_record_vibration.md` — **ЧИТАТЬ ПЕРВЫМ
>   (13.07, САМОЕ СВЕЖЕЕ).** (1) Запись регистратора расширена 24→**48 байт
>   (v2)** — пик+RMS ×2 канала вибрации, среднее+пик скорости, версия/вариант,
>   готово под вариант B (2 accel); формат ПОДТВЕРЖДЁН на железе. (2) Вибрация в
>   записи была нулями — причина оказалась **НЕ кэш сборки, а БАГ кода вибрации,
>   ВЕШАВШИЙ регистратор** (не отвечал по LTP). Убрал вибрацию → устройство ожило.
>   Сейчас прошит **УРЕЗАННЫЙ `Data.c`** (v2-запись БЕЗ вибрации, рабочий; заготовка
>   `Data_CLAUDE_new.c` в корне). Вибрацию возвращать ПО ШАГАМ (план в §0 заметки).
>   Логика вибрации доказана на «Мониторинге» (ПК-расчёт живого accel). (3) **✅
>   ИНДИКАТОР ВЕРСИИ ПРОШИВКИ** — `com.c::cmdWhoAmI` шлёт `[IMU_id, ГГ,ММ,ДД,ЧЧ,ММ]`
>   + имя; LOGLSMW показывает на «Данные» (группа «Параметры регистратора»):
>   **`LogLSMa`** (финальная «a» СТРОЧНАЯ — разделитель модулей: `LogLSMaE00`) +
>   **«Версия ПО: ГГ.ММ.ДД ЧЧ:ММ»**. ⚠ Версия = **5 `#define FW_YY/MM/DD/HH/MI`
>   РУКАМИ в `com.c`** (НЕ авто-`__TIME__`, НЕ CMake — отвергнуто). **Инвариант:
>   версию меняет ТОЛЬКО автор кода при правке прошивки; любое расхождение версии
>   = аномалия (старый ELF/недокат сборки) — стоп, ищем причину вместе.** Имя
>   хранится в контроллере. Формат записи — `data_format_spec_v1.md` §«v2».
> - `actual/session_notes_2026-07-12_gyro_throttle.md` — **ЧИТАТЬ ПЕРВЫМ
>   (12.07 ВЕЧЕР).** (1) ODR глубокого сна **1.6 → 26 Гц LP
>   (~7 мкА, ×24)** — откат утреннего 1.6: на стенде «Тест»@104 ловит 8 об/мин,
>   а «Работа»@1.6 — нет → причина в ODR детектора, 26 = рабочая точка (1.6 — до
>   полевой валидации). (2) **Гироскоп-троттл Пассивный→Активный с эскалацией**
>   (1→16 с): accel слушаем всегда, дорогой гироскоп включаем редко; троттл ТОЛЬКО
>   на deep-wake (не «Проверку»), эскалация на idle-сессию (не на промах CONFIRM) —
>   иначе резал старт (был −8 с, исправлено; 8–200 об/мин дали 0/±1 с). (3) Термины
>   режимов **Пассивный/Активный/Сервисный/«между землёй и небом»**. (4) **«Тест» =
>   ЭТАЛОН** времени (ровно 0 с; «Работа» ±1 с — квантование старт-по-пробуждению,
>   в норме, доводка — в «Калибровку»). (5) **Standby — НЕ делаем** (Stop2 ~7 мкА
>   финал). Чистая спека — `work_cycle_spec_v1.md` **v1.1**.
> - `actual/session_notes_2026-07-12_accel_current_odr.md` — **⚠ ЧАСТИЧНО
>   УСТАРЕЛА** (утро 12.07): описывает коммит **1.6 Гц LP**, который ВЕЧЕРОМ
>   ОТКАЧЕН на 26 Гц (см. заметку выше). Актуальны разборы токов/лесенки ODR и
>   «пропуск 1-го цикла — баги нет». Ток accel снижался по лесенке 417 HP (170 мкА)
>   → 1.6 Гц LP (4.5 мкА) вместе с **`WK_THS=1`** и **`deepSleep=1`**. На
>   стенде 1.6 Гц ловит чисто (0 с) **с 8 об/мин**; ниже — тихий стенд (худший
>   случай по вибрации, ниже целевых 10), в поле порог уедет вниз. **Рабочая
>   точка теперь 26 Гц LP (7 мкА)**, не 1.6.
>   **«Пропуск первого цикла» — БАГА НЕТ:** артефакт возрастающей циклограммы
>   (1-я позиция = самая низкая скорость = слабая вибрация); на постоянной
>   скорости 1-й цикл ловится всегда. `deepSleep=0` vs `=1` стоит ровно ОДИН
>   первый цикл (−2 с надёжно vs 0 с с риском холодного accel-wake) — выбран
>   `=1`. Скорость −7…−11% (`SPEED_CALIB_COEF=1.0`) — открыто, в «Калибровку».
>   Дальнейшее снижение тока датчика — практически предел (см. §1 заметки/iNemo).
> - `actual/session_notes_2026-07-11_work_mode_bench.md` — **ЧИТАТЬ ПЕРВЫМ
>   (11.07).** Режим **«Работа»** (0x1D) ДОВЕДЁН НА СТЕНДЕ до **целевой
>   автономной схемы** (3 уровня). Финал: **ГЛУБОКИЙ СОН** (Stop2, accel
>   Wake-Up/EXTI13 на **104 Гц LP ~17 мкА**, БЕЗ таймера) → по движению accel-INT
>   будит → **«ПРОВЕРКА»** (RTC ~2 c + гироскоп, EXTI13 не арм) → вращение =
>   ROTATING (запись+пуш); долго пусто (`IDLE_CHECKS_TO_DEEP`) → обратно в
>   глубокий сон. Опроса в покое НЕТ. Ключевое: **старт цикла = момент
>   пробуждения** («нет мёртвого времени») + старт в глубоком сне → интервалы
>   **0 c на ВСЕХ циклах** (как «Тест»), «Общее» без дрейфа, ток ×10.
>   Подтверждено многоцикловыми прогонами (10…300 об/мин, включая холодный старт
>   и пробуждение после пауз на 104 LP). `Poll_Sensor` ждёт **DRDY гироскопа**
>   (не accel) → ODR accel на измерение не влияет. Остаток — только СКОРОСТЬ:
>   стабильный −5.4%@74 (устоявшийся, не края — 60 c дал те же −5.4%) → снят
>   **калибровочным коэффициентом** `SPEED_CALIB_COEF=1.057` (Data.h/
>   HandleSensorData), 74→+0.0% подтверждено; на др. скоростях компромисс
>   одноточечного k, замена — многоточечная калибровка. Диагностика 0x7E
>   вычищена (прошивка+LOGLSMW). **ДВОЙНОЙ корень эпопеи «Работа не
>   просыпалась»:** (а) LOGLSMW в ДВУХ слоях глушил unsolicited-метки кроме
>   `CYCLE_PUSH` (были слепы); (б) EXTI13 на 417 HP давал спин при арме во время
>   активности — решено армить EXTI13 ТОЛЬКО в глубоком сне (мотор стоит, спама
>   нет). Полная сводка правок и статусов — разделы «HANDOFF»/«ЦЕЛЕВОЙ АВТОМАТ»
>   в файле. ОСТАЛОСЬ: единая вкладка «Калибровка» (время RTC + коэф. скорости +
>   параметры, план в speed-заметке §10); FSM-трек автономности (iNemo).
> - `actual/work_cycle_spec_v1.md` — **ИТОГОВАЯ СПЕЦИФИКАЦИЯ рабочего цикла**
>   (чистое описание, без истории отладки): трёхуровневый автомат «Работа»
>   (Глубокий сон / Проверка / Вращение) + контроль кабеля (сервисное окно),
>   принципы, параметры, «Работа» vs «Тест», реализация по файлам. Опорный
>   документ по циклу — читать за целостной картиной алгоритма.
> - `actual/wake_architecture_v1.md` — **ОПОРНЫЙ по подсистеме пробуждения /
>   живучести** (12.07). Источники wake (PA0/PC13/EXTI13/RTC), ярусы сна,
>   и главное — как гарантированно НЕ уснуть навсегда в поле: **IWDG**
>   (заморожен в Stop, стережёт живой код, бьёт сбросом → recovery) +
>   **медленный RTC-backstop ~1 ч** (резюмит Stop2 без сброса, переарм accel,
>   вытаскивает из мёртвого источника пробуждения, ~0.07 мкА) + **reset-recovery**
>   (backup+Flash-stub — реакция на аварийный сброс, НЕ штатный режим).
>   §1–2 реализованы, §3–5 спроектированы (в прошивке пока нет). RTC-wake в
>   Stop2 подтверждён на железе. Standby НЕ используется (см. заметку 12.07).
> - `actual/calibration_spec_v1.md` — **ОПОРНЫЙ по калибровке** (12.07). Принцип:
>   вся калибровочная аппаратура на СТЕНДЕ, в приборе — ничего (ни GPS), только
>   хранение+применение коэффициентов (LSE, TMP117, внутр. Flash). Время: RTC
>   smooth-calib (`RTC_CALR`, окно 32 с = 2²⁰, шаг 0.954 ppm) — постоянный член
>   от GPS-PPS на стенде через вывод **RTC_CALIB** сервисного разъёма (заложен);
>   температурный член β·ΔT² по TMP117 (перезапись на ходу); посев абс. времени
>   GPS UTC. Скорость: многоточечная таблица + темп-поправка (`SPEED_CALIB_COEF`
>   сейчас 1.0). Нужны LTP-команды калибровки (НЕТ) + вкладка «Калибровка»
>   (заглушка). ПЛАН, в коде почти ничего.
> - `actual/session_notes_2026-07-11_speed_avg_axis_fix.md` — измерение
>   «Скорости» регистратора: **пик → среднее** по фазе вращения; **модуль
>   вектора** гироскопа √(x²+y²+z²) вместо одной оси X (устойчиво к наклону
>   45°); округление; пропуск разгонной/хвостовой выборок. Подтверждено на
>   железе (10→10, остальное −2…−3%). §10 — идея **единой процедуры
>   калибровки** + добавлена вкладка-заглушка «Калибровка» (`tabCalibration`)
>   в LOGLSMW.
> - `actual/session_notes_2026-07-04_test_mode.md` — **ЧИТАТЬ ПЕРВЫМ.**
>   Реализован и подтверждён на железе режим **«Тест»** (`CMD_START_TEST`
>   0x23, флаг `testNoSleep`, бодрая SLEEP-ветка main.c по флагу WUF2 —
>   без Stop2): колонка «Регистратор» на «Стенде» заполняется живьём.
>   Терминология Тест/Работа зафиксирована (2 команды: 0x23 без сна /
>   0x1D со сном, стоп общий 0x22); сон «Работы» = **Standby 280 нА**, не
>   Stop2 (исправлено противоречие в таблице «Режимы работы»); оба пина
>   PA0/PC13 = SYS_WKUP. Курсор записи Flash — **без кэша**, всегда
>   `flashFindWritePos` (после стирания пишет со стр.1 без перезагрузки).
>   UI: вкладка «Стенд» — 3 кнопки **Журнал/Тест/Работа**; «Мониторинг»
>   перестроен (3 ряда, индикаторы T под чекбоксами, правый блок вправо);
>   панель сравнения — 3 цикла, **лок-степ + «нет ответа»** (строка ждёт
>   пуш; пропуск регистратора виден меткой, столбец больше не съезжает —
>   подтверждено на малых скоростях). Lifetime «Общая» — тоже **без кэша**:
   стирание чипа обнуляет `totalSec` НАПРЯМУЮ. «Циклов» = завершённые стенд-
>   циклы за сессию; «Сессия» = только ОБРАБОТАННЫЕ ответы (нет ответа → 0,
>   без фантомов). Панель **«Память устройства»** финальная: бар (белая
>   сеточка, адаптивный масштаб) + «Свободно», внутри общего правого окна;
>   кнопки Память/Образ/Журнал под окном вывода; история **200 циклов** +
>   прокрутка (весь прогон); Старт/Стоп со стенда всегда; дельта «N с».
>   **✅ ДВА САМОВОССТАНОВЛЕНИЯ БЕЗ ПИТАНИЯ подтверждены на железе:**
>   (1) **QPI-сброс** `P25Qx_Reset` (0x66/0x99→SetQuadSpi→SetQPI) в
>   `P25Qx_Init` — чип сам выходит из QPI после SWD-прошивки; (2)
>   **Poll_Sensor таймаут** DRDY (500 мс) — регистратор не виснет на высоких
>   об/мин (прогон до 268 ОК). Принцип: **НИКАКИХ обесточиваний** (батарейка
>   в поле). **ОТКРЫТО:** скорость reg>стенд (+%, пик на рампе) + дробление
>   регистратора (durS=0/1 фрагменты) — отложено (см. хвост заметок).
> - `actual/session_notes_2026-07-03_stend_bench_a_b.md` — **обе сессии
>   03.07.** Сессия 1: вкладка «Стенд» после A/B починена и
>   подтверждена (гейт `!monitoringActive`, `CMD_STOP_REGISTER` 0x22 +
>   `ComPoll()` в бодрых фазах, 0x1D/0x22 из LOGLSMW); **Wake-Up LSM6DSO
>   не работает в ULTRA_LOW_POWER**, на 417 Гц HP работает
>   (подтверждено). Сессия 2: 26 Гц LP — НЕ работает (подтверждено), в
>   прошивке кандидат **104 Гц LP — пуши на нём ещё не подтверждены**;
>   стоп 0x22 во время вращения работает; **офлайн-вид «Стенда» готов и
>   проверен** (Память…/Образ…/Журнал…/Очистить; Регистратор=только
>   Flash, Стенд=живой|журнал|образ; целостность образ↔память — нулевые
>   дельты на 1000 записей); UI-перестройка вкладки; кнопка «Старт
>   стенда» на Мониторинге (noReg-циклограмма: мотор по программе,
>   регистратор в Service — тестовый режим датчика, последний фикс не
>   проверен). **Архитектура сна согласована**: целевой PASSIVE =
>   STANDBY 280 нА, двухъярусно со Stop2 в рабочих паузах;
>   backup-регистры ИСПОЛЬЗУЮТСЯ (0xBEBE-маркер часов, rp_device
>   reset.c — в LOGLSMA перенести); возврат в Standby — большой
>   временной лаг (TBD). QPI-инсайт: после SWD-прошивки Flash-чип
>   остаётся в QPI — передёргивать питание (TODO прогр. сброс 0x66/0x99).
> - `actual/session_notes_2026-07-02_passive_wkup1_bug.md` — **ЗАКРЫТА**
>   (02.07, сессия 2): устройство снова на связи, подтверждено на железе
>   (после перепрошивки + перещёлкивания тумблера питания). Реализована
>   архитектура режимов A (автономный автомат SLEEP→CONFIRM→ROTATING,
>   Stop2, запуск только командой CMD_START_REGISTER/0x1D) и B
>   (`Service()`, вход по уровню WKUP1) — согласована, не переоткрывать;
>   перенесена в раздел «Режимы работы» ниже. Причина «команды не
>   проходят» была ДВОЙНАЯ, обе части — потери одной и той же регенерации
>   `.ioc` (см. «⚠️ Регенерация .ioc» в правилах Firmware ниже):
>   (1) `HAL_GPIO_Init` PA0/PC13 выпал из `gpio.c` → `HAL_GPIO_ReadPin`
>   на PA0 недостоверен (Analog-режим) → вход в Service() по уровню не
>   срабатывал — исправлено в `main.c`/`USER CODE BEGIN 2` (оба пина);
>   (2) `USART2.BaudRate` откатился 921600→115200 (`usart.c` + `.ioc`) —
>   тишина даже при рабочем входе в Service(); возвращено 921600.
>   Заодно восстановлен выпавший там же USER CODE блок USART1
>   (BLE) в `HAL_UART_MspInit`/`MspDeInit` (`usart.c`).
>   НЕ проверено на железе (TODO следующих сессий): сам Stop2 (ток ~2 мкА,
>   пробуждение по WKUP2/акселерометру, автомат A целиком), порог
>   Wake-Up датчика (дефолт threshold=0x02/duration=0 не калиброван,
>   Enable_Wake_Up_Detection задирает ODR акселерометра до 417 Гц — в
>   `lsm6dso_init()` он возвращается на 12.5 Гц сразу после, проверить,
>   что детект при этом реально работает).
> - `actual/session_notes_2026-07-02_flash_and_reg_column.md` — заметки
>   более ранней части дня (02.07). Заметки восстановлены постфактум (сессия прервалась
>   из-за лимита контекста, писались не в реальном времени). `SaveParamOnEEPROM`
>   в прошивке LOGLSMA перестала быть заглушкой — реально пишет 24-байтные
>   записи циклов в NOR Flash (CRC16, `flashFindWritePos` линейный скан).
>   `LoadTotalSec` восстанавливает lifetime-наработку из последней Flash-записи.
>   В LOGLSMW: правая колонка «Регистратор» в `cmpReport` теперь реально
>   заполняется из CMD_CYCLE_PUSH (`stendFillRegColumn`, позиционный матчинг,
>   не по времени); реализован двоичный поиск первой свободной страницы Flash
>   (`flashBinSearchStart` — закрывает старый пункт «Идеи на будущее» про
>   «Образ: страниц/начало»); добавлена панель «Память устройства»
>   (Наработка/Сессия/Циклов). Названия уточнены 02.07 вечером — путались
>   визуально («Общее» в панели устройства vs «Общая» в правой панели
>   cmpReport): поле панели устройства переименовано в «Наработка», а
>   метка в cmpReport/файле журнала — из «Общая» в «Общее» (согласование
>   рода с остальными метками не требовалось, отличие было чисто
>   визуальное). Открыто:
>   `CMD_RESET_TOTAL` (0x21) реализован в прошивке, но не вызывается с
>   ПК — не разобрано, сознательный задел или недоделанный откат;
>   `maxVibro` в Flash-записи всегда 0.0 (акселерометр не подключён к подсчёту).
> - `actual/session_notes_2026-07-01_journal_and_config.md` — Журнал стенда на диск ✅, кнопка «Открыть журнал…» ✅,
>   удалён chkStendNoRead, формат файла с `|`, дефолты циклограммы исправлены.
>   Зафиксирован план блока конфигурации (min_cycle_duration, GYRO_THRESHOLD,
>   noDetectCount, FS, ODR, режим записи). Хранилище: MCU EEPROM (паспорт + конфиг,
>   переживает стирание Flash); NOR Flash header — только оперативное.
> - `actual/session_notes_2026-06-30_stend_cyclogram_complete.md` — циклограмма
>   стенда завершена: все 4 режима ✅,
>   CMD_CYCLE_PUSH работает, правая колонка Регистратор заполняется.
>   Ключевые правки: RTC_SubTimeDateSec реализован, GYRO_THRESHOLD=600,
>   дебаунс noDetectCount=3, prescaler+1 в StepDriver, дельта скорости в %.
> - `actual/session_notes_2026-06-30_registrator_fixed.md` — Регистратор
>   (LOGLSMA): **починен** (30.06.2026 утро). Причина молчания —
>   `MX_USART2_UART_Init()` вызывалась при 4 МГц до переключения тактовой,
>   BRR=4 < BRR_MIN=16, `HAL_UART_Init()` → `Error_Handler()`.
>   Исправлено: `ServiceClock_Config()` в `USER CODE BEGIN SysInit` в main.c.
> - `actual/session_notes_2026-06-22_dma_and_registrator_mystery.md` —
>   история поиска бага (закрыта 30.06.2026), DMA-приём стенда (921600) ✅.
> - `actual/session_notes_2026-06-10_firmware_build.md` — состояние firmware, управление питанием
> - `actual/session_notes_2026-06-11_ltp_implementation.md` — LTP в железе ✅,
>   концепция системы (стенд + BLE-мост, адресация 0x8C/0x8D/0x8E)
> - `actual/session_notes_2026-06-12_tz_v2.md` — ТЗ v2 согласовано
>   (`actual/TZ_LOGLSMW_v2.md`), эскиз R21, GET_STATS 0x1E, этап 1 (UI-каркас) сделан
> - Раздел **«Стенд (циклограмма испытаний)»** ниже (21.06.2026, без
>   отдельного session_notes файла): прошивка `Stend/Stend LOGLSM/` готова и
>   собирается, переведена на LTP (0x8C), стенд распаян и проверен; логика
>   циклограммы написана в LOGLSMW (команды на 0x8C), не проверена на
>   реальном железе — плата ещё не перешита.
> - `iNemo/iNemo.md` — **ОТДЕЛЬНЫЙ РАЗДЕЛ/ТРЕК** про встроенный FSM датчика
>   LSM6DSO (переименован из iNemo/CLAUDE.md 11.07.2026). Живёт своей веткой,
>   не разбавляет основной контекст. Цель — перенести детект вращения внутрь
>   чипа (MCU не просыпается на случайную тряску). Проработано 11.07.2026:
>   признак = **устойчивая вибрация** accel-only (не периодичность), под
>   Регистратор (низкий ODR/автономность), двухъярусно FSM→INT1→гироскоп,
>   нижняя граница 10 об/мин. Инструмент — **MEMS Studio** (заменил Unico/
>   Unicleo; связка: свободная Nucleo + X-CUBE-MEMS1 + наш датчик по I2C, мост
>   STEVAL не нужен). Данные/валидация — через **сервисный UART/Logger**, БЕЗ
>   I2C на вращающейся муфте (I2C-на-ротор = backlog). Есть чек-лист
>   подготовки Nucleo (UM3233 §2.6). Дальше — практика по готовности к железу.
>
> **Именование (соглашение)**: «LogLSM» — имя проекта/устройства в тексте;
> «LOGLSM*» заглавными — технические идентификаторы: платы LOGLSMA/LOGLSMB,
> приложение LOGLSMW.

---

## Что это за проект

Автономный инерциальный логгер на базе STM32L433CC. Устанавливается на ось колеса.
Записывает вибрации и вращение в NOR Flash, выгрузка через UART (сервисный кабель) или USB-C (плата расширения B).

---

## Два аппаратных варианта

| | **LOGLSMA + MA00** | **LOGLSMB + MB00** |
|---|---|---|
| Статус | ✅ **Собран, на стенде, отлаживаем** | 📐 Запланирован, не собран |
| IMU | LSM6DSO-MOD (±16g) — WHO_AM_I=**0x6C** | LSM6DSV320X-MOD (±320g) — WHO_AM_I=**0x70** |
| FRAM шина | SPI1 (PA11/PA12/PA15/PB3) | SPI3 (PB4/PB5/PA15/PB3) |
| QUADSPI CLK | PB10 (пин 21) | PA3 (пин 13) |
| QPWR | PA8 (пин 29) | PB10 (пин 21) |
| USB | Нет | PA11/PA12 → USB-C через MB00 |
| Сервисный UART | USART2, PA2/PA3 (XP2) | USART1, PA9/PA10 (XP1+XP2) |

MA00 = модуль расширения A, номер 00 (бывшее название E1 — та же плата).
MB00 = модуль расширения B, номер 00 (USB-C, FRAM, TMP117 на SPI3).

---

## Структура репозитория

```
LogLSM/
├── Firmware/
│   ├── rp_device/       ← ✅ БАЗА ДЛЯ LOGLSMA — уже сконфигурирован под вариант A
│   │   ├── Core/Src/    ← lsm6dso.c, p25q128.c, fm25xx.c, tmp117.c, com.c, wake.c
│   │   │                   Data.c, pwr.c, rtc.c, ble.c — всё рабочее
│   │   └── rp_device.ioc← QUADSPI/USART2/SPI1 пины совпадают с LOGLSMA точно
│   ├── LogLSM/          ← Прошивка LOGLSMB (вариант B, CMake, .ioc под B) — не трогать
│   │   ├── Core/        ← HAL (CubeMX)
│   │   ├── App/Src/     ← lsm6dsv_i2c.c, lsm6dsv_data.c, log_ringbuf.c
│   │   └── LogLSM.ioc
├── Stend/
│   └── Stend LOGLSM/    ← ✅ Прошивка Nucleo-L476 для стенда (CubeIDE, Makefile)
│       ├── Core/Src/    ← StepDriver.c (TIM1 PWM), com_interr.c (LTP+ring buffers),
│       │                   ltp.c (копия из Firmware/LOGLSMA), ring_buffer.c
│       └── Stend LOGLSM.ioc ← USART2 (→ПК), USART3 (→регистратор), TIM1 CH1 (PA8)
├── SoftWare/
│   └── CtrlRVZD/        ← Qt5 приложение, протокол WAKE (прототип, незакончен)
├── Hardware/
│   ├── LOGLSMA.net      ← P-CAD нетлист варианта A (разобран, верифицирован)
│   └── LOGLSMB.net      ← P-CAD нетлист варианта B (разобран, верифицирован)
├── Doc/
│   └── LogLSM Transport Protocol/
│       └── TMP/LTP_PROTOCOL.md   ← Спецификация LTP протокола
├── Scripts/
│   └── build_tz.js      ← Генератор ТЗ (Node.js → docx)
└── actual/              ← Рабочие документы (всегда актуальны)
    ├── A+E1_pinout_from_schematic.md    ✅ закрыт
    ├── B_pinout_from_schematic.md       ✅ закрыт
    ├── cubemx_config_LOGLSMA+MA00.md   ✅ чек-лист пинов для CubeMX
    ├── data_format_spec_v1.md           ✅ финальный
    └── session_notes_2026-06-10_firmware_start.md  ← ЧИТАТЬ ПРИ СТАРТЕ
```

---

## Протокол связи — LTP

Используем **LTP (LogLSM Transport Protocol)** — эволюция WAKE.

```
Пакет: FEND | ADDR | CMD | FLAGS | SEQ | LEN | PAYLOAD | CRC16

FEND  = 0xC0   (маркер фрейма)
FESC  = 0xDB   (escape)
TFEND = 0xDC
TFESC = 0xDD

CRC16-CCITT: poly=0x1021, init=0xFFFF, no reflect, xor=0x0000
```

**Скорости UART:**

| UART | Скорость | Обоснование |
|---|---|---|
| USART1 → HLK-B40 (BLE) | **115 200 бод** | BLE-эфир ≈ 30–50 KB/s, UART не узкое место; достаточно для графиков и команд |
| USART2 → XP2 (сервисный кабель, регистратор LOGLSMA) | **921 600 бод** | Поднято 22.06.2026 (было 115200, см. ниже) — статический дефолт в прошивке, без живого переключения |
| USART2 (к ПК) / USART3 (к регистратору) на стенде (Nucleo-L476) | **921 600 бод** | Поднято 22.06.2026 после перехода приёма на DMA (было временно снижено до 230400 — см. «DMA-приём на стенде» ниже) |

HLK-B40 поддерживает до 921600 (подтверждено даташитом). Скорость меняется AT-командой.  
Sleep-пины HLK-B40 (PC6/PC7) не разведены на MCU — BLE переводится в off через B_PWON (PB6, p-FET).

**Скорость сервисного UART поднята с 115200 на 921600 (22.06.2026).** Мотивация —
вкладка «Мониторинг» (см. ниже): на высоких об/мин стенда опрос гироскопа/
акселерометра по 115200 не успевал за реальной частотой вращения (видна
пилообразная, а не гладкая картина даже там, где физика гладкая — см.
раздел «Мониторинг» ниже). Решено (обсуждали живое переключение по
команде `SET_BAUD`/0x0B, которая в прошивке уже существует и работает —
`com.c::cmdSetBaud`) **не делать живое переключение**, а поднять статический
дефолт в обеих прошивках и перешить — проще и без гонок по времени на
переключении UART прямо во время передачи (живой вариант для топологии
стенда требовал бы доп. разбора CMD в байтовой ретрансляции `com_interr.c`,
которая сейчас слепая по ADDR, — посчитали лишним риском).
Изменено везде, синхронно (иначе разные звенья перестанут понимать друг
друга — UART не поддерживает автоопределение скорости):
- `Firmware/LOGLSMA/Core/Src/usart.c` (`MX_USART2_UART_Init`) + `LOGLSMA.ioc` (`USART2.BaudRate`) — сервисный UART регистратора.
- `Stend/Stend LOGLSM/Core/Src/main.c` (`MX_USART2_UART_Init`/`MX_USART3_UART_Init`) + `Stend LOGLSM.ioc` — обе линии стенда (к ПК и к регистратору-ретранслятору).
- `SoftWare/LOGLSMW/mainwindow.cpp` — дефолт `cmbBaud` при первом запуске (без сохранённой настройки) теперь 921600.
- `test_ping_stend.bat` — ручной PowerShell-пинг стенда, скорость в скрипте обновлена.

**Важно:** после перешивки обоих устройств они понимают ТОЛЬКО 921600 —
без отдельного обновления всех тестовых скриптов/настроек, ожидающих
115200, связи не будет (UART — фиксированная скорость, не автоопределение).

**Найден и исправлен сопутствующий баг ретрансляции (22.06.2026, всплыл
при проверке 921600, но существовал и на 115200 — просто не был замечен).**
Симптом: прямой одиночный PING на 0x8C через `test_ping_stend.bat` проходил
идеально (ACK байт-в-байт совпадал с ожидаемым), а сканирование из LOGLSMW
(шлёт PING на 0x8D и 0x8C подряд, см. `PortScanner`) — нет, с разным числом
мусорных байт на приёме при каждой попытке (12, 13…), 0 ошибок CRC (мусор не
доходил до разбора пакета). Причина: `PC11` (USART3_RX, линия стенд→
регистратор) была настроена `GPIO_NOPULL` — без подключённого регистратора
вход висел в воздухе и ловил наводку; `com_interr.c` ретранслирует ВСЕ байты
с этой линии на ПК не глядя (см. `HAL_UART_RxCpltCallback`, ветка `huart3`),
включая случайный шум. PING на 0x8D в составе сканирования ненадолго
включает приём на этой линии (ожидая возможный ответ от регистратора) —
оттуда и мусор именно при сканировании, а не при одиночном PING на 0x8C
(тот вообще не трогает relay-логику). Исправлено: `PC11.Pull` →
`GPIO_PULLUP` в `stm32l4xx_hal_msp.c` (+ синхронно в `.ioc`,
`PC11.GPIO_PuPd=GPIO_PULLUP`) — линия в покое держится HIGH, не плавает.

**Второй, более серьёзный баг найден следом (22.06.2026) — потеря самого
ACK на 0x8C, не просто мусор.** После фикса PULLUP сканирование из LOGLSMW
всё равно не находило стенд (то 0, то N байт мусора на разных попытках).
Проверено в обход Qt/LOGLSMW: одиночный PING на 0x8C (`test_ping_stend.bat`)
проходит идеально каждый раз; два PING подряд без паузы (0x8D, затем 0x8C —
именно так делает `PortScanner`, см. `test_ping_dual.ps1`/`.bat`, с байтами
пакетов, посчитанными и сверенными через Python с тест-вектором CRC из
`ltp.c`) — ACK на 0x8C **не приходит вообще**, хотя сам по себе он
гарантированно рабочий. Причина: приём USART2 на стенде — побайтовый, по
прерыванию (`HAL_UART_Receive_IT(..., 1)`), не DMA (в отличие от
регистратора, у которого `HAL_UARTEx_ReceiveToIdle_DMA`). На 921600
(~11 мкс/байт) и тактовой частоте всего 16 МГц (HSI без PLL) обработчик не
успевает обработать релей-ветку первого пакета (PING на 0x8D — чужой адрес,
лишняя логика поиска UART и постановки в кольцевой буфер) и переустановить
приём до прихода первого байта второго пакета — тот пропадает (overrun),
ломая разбор всего второго пакета.

Исправлено **на стороне ПК, не прошивки** (дешевле и безопаснее, чем
переводить приём стенда на DMA): `PortScanner::sendPings()`
(`portscanner.h/.cpp`) теперь шлёт PING на алгоритм-адрес сразу, а PING на
альт-адрес — через `QTimer::singleShot(5, ...)`, не впритык. Сценарий «два
разных пакета без ожидания ответа между ними» возникает только при
сканировании порта — в обычной работе (Мониторинг, Стенд, Тест памяти)
команды и так идут по одной, с ожиданием ответа/таймаута, поэтому пауза
ничего не блокирует в штатной работе. DMA-приём на стенде (по образцу
регистратора) — более капитальное решение, в этот проход не делали,
зафиксировано как возможное улучшение на будущее, если когда-нибудь
понадобится слать на стенд несколько команд впритык без ожидания ответа.

**Третий, итоговый вывод (22.06.2026) — стенд не держит 921600 вообще, ни
в каком сценарии, даже одиночный PING без сканирования.** После фикса
PortScanner (выше) проверка одиночного PING на 0x8C через чистый
PowerShell-скрипт (`test_ping_stend.bat`, без Qt/LOGLSMW) внезапно стала
возвращать 0 байт — притом что физическая плата подтверждённо прошита и
выполняет код (доказано временным heartbeat на busy-counter без
зависимости от HAL_GetTick — диод мигал). Долгий перебор переменных по
одной (PULLUP/NOPULL на PC11, разные физические платы Nucleo, физическое
переподключение USB-кабеля, COM4 vs COM5 в Device Manager, CubeIDE Run vs
Debug vs отдельный STM32CubeProgrammer для прошивки) ничего не дал — при
921600 и при 460800 одиночный PING стабильно не возвращал ничего (на
460800, уже после восстановления PULLUP, — честный 0 байт без мусора, что
само по себе подтвердило, что PULLUP-фикс баг №1 отдельно работает
корректно). Спуск на **230400** — сразу рабочий результат: PING отвечает,
LOGLSMW подключается и идентифицирует устройство, команда `STEND_START`
проходит («старт циклограммы» в логе).

Причина — та же, что и у бага №2 выше (общий корень, не новая находка):
приём/передача USART на стенде побайтовые, по прерыванию
(`HAL_UART_Receive_IT(..., 1)`), не DMA, на тактовой частоте всего 16 МГц
(HSI без PLL — это текущая настройка `SystemClock_Config()`
(`PLL.PLLState=RCC_PLL_NONE`), не предел кристалла: L476 умеет до 80 МГц
через PLL, просто тактирование под это не настроено). На регистраторе (LOGLSMA) аналогичная служба работает на
921600 устойчиво, потому что там `HAL_UARTEx_ReceiveToIdle_DMA` — приём не
зависит от того, успевает ли обработчик прерывания между байтами. На
стенде это ограничение оказалось не только при двух пакетах впритык
(баг №2, уже пофикшен паузой 5 мс на ПК), а фундаментальным даже для одного
изолированного пакета — на 921600 (~1.1 мкс/бит) и 16 МГц обработчик
прерывания просто не успевает отработать `HAL_UART_RxCpltCallback` и
перевзвести приём между байтами одного и того же пакета. 460800 — та же
история (~2.2 мкс/бит всё ещё мало). 230400 (~4.3 мкс/бит) — достаточный
запас.

**Решение: для стенда статический дефолт снижен до 230400** (обе линии —
USART2 к ПК и USART3 к регистратору-через-ретранслятор), отдельно от
регистратора (LOGLSMA), который остаётся на 921600 (DMA-приём, эту
проблему не имеет). Изменено:
- `Stend/Stend LOGLSM/Core/Src/main.c` (`MX_USART2_UART_Init`/`MX_USART3_UART_Init`) — `BaudRate=230400`.
- `Stend LOGLSM.ioc` — `USART2.BaudRate=230400`, `USART3.BaudRate=230400`.
- Временный диагностический heartbeat (busy-counter + мигание LD2 в `main()`) убран — он сослужил свою роль (подтвердил, что ядро выполняет код независимо от UART/таймеров) и больше не нужен; `main.c` снова чистый CubeMX-вид с `ComPoll()` в цикле.
- `PC11.Pull=GPIO_PULLUP` (баг №1 выше) подтверждён и оставлен как есть — не имел отношения к этой находке, но и не мешает.

**DMA-приём на стенде реализован (22.06.2026) — возврат на 921600.**
Вместо того чтобы оставаться на 230400, перевели приём обоих UART стенда
на `HAL_UARTEx_ReceiveToIdle_DMA` по образцу регистратора (`Firmware/LOGLSMA/App/Src/com.c`).
DMA-периферия сама забирает байты в буфер без участия CPU, поэтому скорость
больше не ограничена тем, успевает ли обработчик прерывания между байтами —
корень проблемы (см. «Третий, итоговый вывод» выше) снят полностью, без
необходимости трогать тактовую частоту (PLL).

Сделано через CubeMX/CubeIDE (`.ioc`, GUI): добавлены DMA-запросы
`USART2_RX` (DMA1 Channel 6) и `USART3_RX` (DMA1 Channel 3), режим Circular
(TX отдельно не добавляли — TX остаётся на прерываниях через кольцевой
буфер, он не был источником проблемы). После генерации кода CubeMX:
- `main.c` — добавлен `MX_DMA_Init()` (вызывается перед `MX_USART2_UART_Init()`/`MX_USART3_UART_Init()`), глобальные `hdma_usart2_rx`/`hdma_usart3_rx`.
- `stm32l4xx_hal_msp.c` — линковка DMA в `HAL_UART_MspInit()` для обоих UART; PC11 пересоздан с `GPIO_PULLUP` — фикс бага №1 пережил регенерацию, синхронно с `.ioc`.
- `stm32l4xx_it.c` — добавлены `DMA1_Channel3_IRQHandler`/`DMA1_Channel6_IRQHandler` (вызывают `HAL_DMA_IRQHandler`).

Дальше вручную (`App`-уровень, CubeMX не трогает): `Core/Src/com_interr.c`
переписан — два буфера приёма (`RxBufPc[256]`/`RxBufReg[256]`), запуск через
`HAL_UARTEx_ReceiveToIdle_DMA` в `ComInit()`, рестарт DMA после каждого IDLE
отложен в `ComPoll()` (не из ISR, как и у регистратора). Relay-логика (байт
от ПК с ADDR≠0x8C форвардится на регистратора-линию, байт от регистратора
форвардится на ПК) сохранена 1:1 по смыслу со старой ISR-версией, но теперь
работает по байтам из буфера (`HAL_UARTEx_RxEventCallback`), а не по
аппаратному прерыванию на каждый байт — поэтому больше не требует успевать
переустановить приём между байтами. Динамическое открытие/закрытие приёма на
huart3 под конкретный relay-пакет (было в старой версии) убрано как
ненужное — DMA-приём на huart3 теперь всегда активен независимо.

Статический дефолт стенда вернули на **921600** (`main.c` + `.ioc`,
синхронно с регистратором). 230400 оставлен только как зафиксированный в
истории промежуточный результат диагностики, не текущее значение.

Таблица команд (минимум для старта):

| CMD | Название | Описание |
|---|---|---|
| 0x01 | CMD_PING | Пинг — ответ ACK |
| 0x02 | CMD_WHO_AM_I | Идентификатор устройства |
| 0x10 | CMD_FLASH_PAGE_TEST | Тест страницы Flash |
| 0x11 | CMD_FLASH_ERASE | Стереть Flash |
| 0x20 | CMD_IMU_READ | Данные IMU |
| 0xFF | CMD_ERROR | Ошибка |

Спека: `Doc/LogLSM Transport Protocol/TMP/LTP_PROTOCOL.md`

---

## Стенд (циклограмма испытаний) — статус и план

> Обсуждалось 11.06.2026 (`session_notes_2026-06-11_ltp_implementation.md`,
> раздел «На будущее: технологический блок», помечено «в ТЗ пока не
> вносить» — отсюда и терялось между сессиями). Восстановлено и сведено
> сюда 20.06.2026.

**Железо:** стенд = драйвер шагового двигателя (готовый, современный,
управляется импульсами — 1 импульс = 1 шаг) + сам шаговик (200 шагов/оборот)
+ контроллер **Nucleo-L476** (другая плата, не LOGLSMA — STM32L433 там, а
тут L476), подключённый к ПК через VCP (виртуальный COM ST-Link).
Дробление шага — **DIP-переключатели на самом драйвере**, не команда по
протоколу: для софта это просто константа для расчёта частоты импульсов
под нужную скорость (об/мин ↔ Гц), реально на стенд не передаётся.
Назначение — 2 канала (2 шаговика), не один.

**Топология линии связи (решено 11.06.2026):** VCP Nucleo — единственный
порт к ПК, второй порт LOGLSMW не нужен. Регистратор (LOGLSMA) физически
подключён к Nucleo через второй UART (он же служебный разъём, куда обычно
подключают программатор Nucleo). Nucleo разбирает входящие LTP-пакеты по
ADDR: свои (управление шаговиком) обрабатывает сам, чужие — ретранслирует
на второй UART к регистратору. Схема 1-в-1 повторяет планируемый
BLE-ретранслятор (ТЗ §1.1) — маршрутизация по ADDR пакета LTP.
**Адреса:** Nucleo/стенд = `0x8C`, регистратор (LOGLSMA) = `0x8D` (уже
действующий, см. `LTP_DEV_ADDRESS` в `com.h`), удалённый по BLE = `0x8E`.

**Зачем на одной линии (подтверждено пользователем 11.06.2026):** старый
подход проверки регистратора — сравнение двух раздельных логов (лог
стенда + лог регистратора, разные форматы, ручная сводка) — был крайне
неудобен, хотя в принципе работал. Замысел LOGLSMW — единый журнал
испытания на общей шкале времени ПК: приложение само пишет и циклы стенда,
и данные регистратора в одну таблицу, с автосопоставлением «стенд задал
N об/мин ↔ гироскоп регистратора показал соответствующее значение» — это и
есть вкладка «Стенд» (`tabStend` в `mainwindow.ui`: циклограмма работа/
простой/смена скорости/число циклов, Старт/Стоп, колонки «Стенд
задал»/«Регистратор измерил»/«Расхождение», график `plotStend`, журнал
`tblTestJournal`).

**Был прообраз функционала** — со слов пользователя, ранее существовала
пробная программа управления стендом, отрабатывавшая временные интервалы
запуска шаговика на разной скорости (циклограммы) и вела лог начало/конец
цикла + длительность + скорость. Лог там был **по событиям и относительному
времени физики цикла** (старт вращения на скорости X, потом стоп/отметка),
**не привязанному к реальным часам** — отсюда и трудность сводки с
регистратором, у которого записи на абсолютном Unix-времени (уточнено
20.06.2026). У LOGLSMW эта проблема решается естественно: раз именно ПК
формирует команды стенду, он же ставит реальную метку времени на отправку
каждой команды — общая шкала появляется сама, без необходимости синхронизировать
часы самого стенда отдельно.

Сама программа (или её код) **не найдена
в этом репозитории** — ни папки под Nucleo-L476/`.ioc`-проекта, ни исходников
протокола стенда нигде нет. Пользователь обещал показать эту программу
отдельно.

**Минимальный набор команд Nucleo (уточнено 20.06.2026):** LOGLSMW общается
с 2 устройствами через один порт. Адресовано регистратору (0x8D) — Nucleo
просто ретранслирует пакет на второй UART (служебный разъём) без обработки.
Адресовано самому Nucleo (0x8C, то есть фактически драйверу шагового
двигателя) — Nucleo обрабатывает сам, и набор команд здесь минимальный:
**задать частоту импульсов (= скорость)** или **остановить**. Сама
циклограмма (рандомизация work/pause, выбор скорости в диапазоне, счётчик
циклов — все поля группы «Циклограмма испытания» в `tabStend`) считается и
планируется **на стороне LOGLSMW (ПК)**, а не во прошивке Nucleo — ПК сам
решает, когда отправить очередную команду «частота X» или «стоп» по
сценарию, Nucleo логики циклограммы не содержит.

**Прошивка Nucleo — функционал (уточнено 20.06.2026):** программа,
зашиваемая в Nucleo-L476, должна понимать входящие LTP-пакеты и по ADDR
делать одно из двух: (1) адресовано регистратору (0x8D) — ретранслировать
пакет как есть на второй UART (служебный разъём); (2) адресовано самой
Nucleo (0x8C) — разобрать команду («частота X»/«стоп») и задать нужные
параметры **своему аппаратному таймеру/счётчику** (PWM-канал STM32),
который и генерирует импульсы нужной частоты на вход драйвера шагового
двигателя. То есть генерация импульсов — задача таймера МК, а не
программного цикла.

**Прошивка Nucleo — реализована (21.06.2026), стенд распаян и проверен.**
Проект `Stend/Stend LOGLSM/` (CubeIDE, Makefile, MCU STM32L476RGTx) собирается
без ошибок. `ltp.c`/`ltp.h` скопированы без изменений из
`Firmware/LOGLSMA/App/Src|Inc` (чистый C11, без зависимостей от HAL).
`com_interr.c` реализует приём LTP по USART2 (линия к ПК), побайтовую
маршрутизацию по ADDR (свой `0x8C` → в парсер `ltp_parser_feed`, чужой,
сейчас всегда `0x8D` → ретранслируется как есть на USART3 к регистратору) и
передачу через кольцевые буферы (`ring_buffer.c`) с разруливанием по
`HAL_UART_TxCpltCallback`/`SendUart()`. `StepDriver.c` — TIM1 канал 1 (PWM,
PA8) генерирует импульсы; `StepDriverSetSpeed()` уже делает плавный разгон/
торможение шагами по 200 об/мин каждые 50 мс, если новое значение
отличается от текущего больше чем на 200 — рампа уже есть, отдельно
реализовывать не нужно. Осталось перешить плату (через ST-Link:
STM32CubeIDE Debug/Run As, либо STM32CubeProgrammer — открывает `.elf`
напрямую без конвертации в `.bin`/`.hex`).

**Формат payload CMD_SPEED/CMD_START/CMD_STOP — уже реализован в коде,
закрывает открытый вопрос №1.** `cmdSpeedHandler()`/`StepDriver.c`:
payload `CMD_SPEED` (0x05) = `speed` (uint16, LE) + `coefficient` (uint8) —
и важно: `speed` — это **сразу об/мин**, не Гц. Пересчёт в реальную частоту
импульсов (Гц) на вход драйвера делает сам `SetTimPeriod()` по формуле
`f = speed × 200 × coef / 60` (200 — шагов/оборот при полном шаге,
`coef = 1 << coefficient` — множитель дробления шага). `CMD_START` (0x04) и
`CMD_STOP` (0x06) — без payload. Раз протокол уже говорит в об/мин — вопрос
№2 (единицы в UI) автоматически закрыт тем же выбором: LOGLSMW может
передавать об/мин из UI в payload без пересчёта на своей стороне.

**Сравнение «задал/измерил» — два режима (решено 20.06.2026):** старая
программа сверяла по событиям/постфактум (см. выше — лог стенда и лог
регистратора сводились вручную, без живого мониторинга во время вращения,
по крайней мере по уточнённым данным). Для LOGLSMW решено не ограничиваться
только этим: основной режим — сверка по завершённым записям (журнал стенда
+ запись регистратора из Flash, сведённые по времени ПК), но **дополнительно
стоит добавить живой режим как опцию** — постоянный поллинг гироскопа
регистратора во время самого вращения, для более тщательного теста. Не
обязателен по умолчанию, но не исключается из плана.

**Текущее состояние реализации (обновлено 21.06.2026 — логика циклограммы
написана):** LOGLSMW теперь умеет реально вращать стенд по LTP.

- `DeviceController` (devicecontroller.h/.cpp) научен говорить на ДВА адреса
  одновременно: `m_targetAddr` (регистратор, настраивается в UI, обычно
  0x8D) и `LtpAddr::STEND` (0x8C, константа). Добавлен `enqueueTo(addr, cmd,
  payload, tag)`, `Request` хранит свой `addr`, `transmit()` шлёт по
  `m_current.addr`, `onPacket()` фильтрует и сверяет ADDR/SEQ/CMD так, чтобы
  ответ с одного адреса не мог подтвердить запрос к другому (коды
  STEND_START/SPEED/STOP численно совпадают с FLASH_READ_ID/ERASE/
  PAGE_ERASE регистратора — это нормально, т.к. адреса разные, но в
  `mainwindow.cpp::onResponse()` ответы стенда (`tag == TagStend`) теперь
  разбираются отдельной веткой `stendHandleResponse()` ДО общего
  `switch(cmd)`, чтобы не попасть в обработчик флеш-команд).
- `LtpCmd::STEND_START/STEND_SPEED/STEND_STOP` (0x04/0x05/0x06, как и
  зафиксировано в `com_interr.c`/`StepDriver.c`) добавлены в
  devicecontroller.h. Коэффициент дробления шага для payload STEND_SPEED —
  константа в коде (`kStendMicrostepCoef` в mainwindow.cpp, сейчас 5 → 32
  микрошага), т.к. само дробление задаётся DIP-переключателями на драйвере
  и программно не управляется (см. выше) — но прошивке всё равно нужен этот
  байт для расчёта частоты, отсюда константа вместо поля UI.
- В mainwindow.h/.cpp реализована полная циклограмма work/pause для всех 4
  режимов (`StendMode::RandomPM/OnlyPlus/OnlyMinus/NStartStop`): построение
  ступенек (`stendBuildSteps`, формула Min..Max включительно из раздела
  «Алгоритм циклограммы» выше), фаза «Работа» с N сегментами равной
  длительности (`stendBeginWork`/`stendAdvanceWorkStep`), фаза «Простой»
  (`stendBeginPause`), отдельная ветка для N старт/стоп с треугольным
  обходом ряда ступенек, который строится один раз на весь прогон
  (`stendNextNStartStop`). Цикл идёт по кругу до ручного «Стоп»
  (`stendStop`), который не дожидается ACK — расписание ведёт локальный
  `QTimer m_stendStepTimer`, а не подтверждения от стенда (таймаут команды
  просто попадает в общий лог как обычная строка, без отдельной привязки).
  `btnStendStart`/`btnStendStop` подключены (`setupStend()`), валидация
  диапазонов (Мин>Макс) — при нажатии «Старт». Пока работает только цикл
  стенда — правая колонка «Регистратор» в `cmpReport` (`stendRenderReport()`)
  всегда пуста. *(Состояние на 21.06 — колонка заполняется начиная с
  02.07.2026 через `stendFillRegColumn`, см. заметку в шапке файла.)*
- **Не реализовано (сознательно, отдельные задачи):** чтение записи
  регистратора после «Стоп» (чекбокс `chkStendNoRead` хранится в UI, но
  ни на что не влияет — сам опрос 0x8D после цикла не написан;
  чекбокс впоследствии удалён 01.07 — данные приходят сами через
  CMD_CYCLE_PUSH, отдельный опрос не понадобился); запись
  журнала испытания на диск (решение о текстовом формате принято выше, кода
  нет — *реализовано 01.07, см. заметку в шапке файла*); привязка ответа
  стенда к завершению шага циклограммы (сейчас
  работает «по расписанию», без ожидания ACK — если стенд не подтвердит
  команду, циклограмма всё равно продолжится по локальному таймеру).
- **Проверено на реальном железе 22.06.2026 — рабочий цикл подтверждён.**
  Прошивка `com_interr.c` дополнена ACK-ответами на все 4 команды
  (`sendAck()`, формат — DIR=1, пустой PAYLOAD, тот же SEQ, см.
  `Firmware/LOGLSMA/App/Src/com.c::cmdPing` для образца) — раньше прошивка
  стенда не отвечала вообще ни на одну команду, включая PING, поэтому
  LOGLSMW не мог найти порт без подключённого регистратора.
  **Настоящая причина долгого зависания «диод не мигает, UART молчит» —
  не код и не железо, а файл `Stend LOGLSM.launch`**: там был выставлен
  `DEBUGGER_STOP_AT_MAIN=true` (+ дублирующий `setStopAt=true`) — то есть
  ЛЮБОЙ запуск (что Debug, что Run — оба используют один файл) прошивал
  код верно, но останавливал ядро прямо на входе в `main()` и не отпускал
  никогда, независимо от платы (проверено на двух разных Nucleo-L476RG —
  поведение идентичное, что и помогло исключить версию «дело в конкретной
  плате»). Оба флага переключены на `false` — после этого правки сразу
  заработали. Диагностический код (мигание LD2 на чекпоинтах/в
  Error_Handler, добавлялся временно при поиске причины) убран обратно,
  `main.c` снова чистый CubeMX-вид.
  Финальная проверка: ручной PING на 0x8C через PowerShell/`.bat`
  (`System.IO.Ports.SerialPort`, сырые байты LTP с верным CRC16) получил
  верный ACK `C0 8C 01 01 00 00 00 00 E7 AD`. После этого циклограмма в
  LOGLSMW реально прогнала несколько циклов «Случайно ±» с мотором —
  Старт/Стоп/Интервал/Общая/Скорость в `cmpReport` совпали с ожидаемым
  форматом, мотор крутился по настоящим LTP-командам с ПК.
  Сторона «Регистратор» в отчёте по-прежнему не реализована (см. выше) —
  это отдельная, ещё не начатая задача.

**Общий сценарий работы (согласовано 20.06.2026, без привязки к точным
единицам/байтам протокола — это отдельный, ещё не решённый слой):** одна
программа (LOGLSMW) одновременно и задаёт режим двигателя, и считывает лог
— разделения на «программу управления» и «программу чтения» нет, как в
старом подходе с двумя раздельными логами. По кнопке «Старт» запускается
таймерный сценарий на N циклов: в каждом цикле — фаза работы (выбрать
длительность и скорость из диапазонов по режиму смены, отправить на 0x8C
«частота X», при нескольких сменах скорости за работу — доп. команды
«частота Y» без остановки мотора, в конце — «стоп») и фаза простоя (пауза
заданной длительности, без команд). Каждая отправленная команда пишется в
журнал с реальной отметкой времени ПК (см. выше про общую шкалу времени).
Сравнение «задал/измерил» — сведение этого журнала с записями регистратора
(см. два режима выше).

**Протокол Nucleo — решено (20.06.2026): только LTP, альтернативы нет.**
Отдельного «своего» протокола стенда никогда не существовало (ни кода, ни
прошивки — см. выше), поэтому вопрос «перевести на LTP или оставить свой»
снят сам собой: Nucleo говорит по LTP, маршрутизация по ADDR (0x8C/0x8D) —
как описано выше. Открыт только точный байтовый формат payload команд
«частота X»/«стоп», не сам протокол.

**Открытые вопросы (с 11.06.2026, закрыты 21.06.2026):**
1. ~~Точный байтовый формат payload для «частота X» / «стоп» на 0x8C.~~
   Закрыт — см. «Формат payload CMD_SPEED/CMD_START/CMD_STOP» выше: уже
   реализован в `com_interr.c`/`StepDriver.c`.
2. ~~Единицы в UI — об/мин или Гц?~~ Закрыт — **об/мин**, везде в UI и в
   payload (см. выше, перерасчёт в Гц делает прошивка, не LOGLSMW).
3. ~~Направление вращения~~ — закрыт (21.06.2026): DIR-пин на драйвере
   шагового двигателя физически есть, но к Nucleo не подключён — направление
   и дробление шага задаются перемычками/DIP-переключателями на самом
   драйвере (см. «Стенд (циклограмма испытаний)» выше про дробление шага),
   программно не управляются. Осознанный выбор простоты; при необходимости
   можно будет распаять DIR на свободный GPIO Nucleo и добавить команду —
   расширение, не блокер, маловероятно понадобится.
   Рампа разгона/торможения — уже реализована (`StepDriverSetSpeed()`,
   шаг 200 об/мин / 50 мс).
   **Остаётся открытым:** конкретные профили испытаний (диапазоны скоростей,
   паттерны циклов work/pause, число циклов по умолчанию) — числа не
   выбраны, заполняются позже на этапе реализации циклограммы в LOGLSMW.

**Деление программы (решено 11.06.2026):** одна сборка LOGLSMW на оба
режима — оператор (без вкладок, дашборд) и инженер (Сервис → ключ →
вкладки Команды/Мониторинг/Тест памяти/Стенд/Настройки). Вкладка «Стенд»
— часть инженерной (расширенной) редакции, не показывается оператору.

> **Обновление 21.06.2026:** ссылки выше на `plotStend`/`tblTestJournal`/
> колонки «Стенд задал»/«Регистратор измерил»/«Расхождение» — устарели,
> см. следующий раздел (UI полностью переработан, логика всё ещё не
> реализована).

### Вкладка «Стенд» в LOGLSMW — UI и архитектура данных (сессия 21.06.2026)

**UI «Циклограммы испытания» (слева, `groupCyclogram`, `QGridLayout`,
фиксированная ширина) — финальная раскладка.** Время работы / Время
простоя: строки «Минимум»/«Максимум» (полные слова, не «Мин»/«Макс» —
чтобы не путать с минутами времени), по 2 спинбокса (мин+сек, суффиксы
« мин»/« сек» внутри поля). Смен скоростей и Скорость, об/мин — по 2 голых
спинбокса (Мин/Макс) без подписей: порядок «левый ≤ правый» самодостаточен.
Все 12 спинбоксов одного размера (90px, без стрелок —
`QAbstractSpinBox::NoButtons`) — иначе при разном содержимом (число с
префиксом/суффиксом против простого числа) колонка визуально «гуляет».
Режим — 4 радиокнопки: «Случайно ±» / «Только +» / «Только −» /
«N старт/стоп» (последний — отдельный треугольный алгоритм: N шагов вверх
от Мин до Макс, потом N шагов вниз, по кругу до ручного Стоп; остальные
три — смена скорости без остановки мотора в рамках фазы работы). Строка
«Дробление шага» убрана из UI совсем (была нередактируемая статичная «32»,
без смысла — шаг и направление всё равно задаются перемычками на драйвере,
не программно, см. открытый вопрос №3 выше). Чекбокс «Не считывать данные
с регистратора» (`chkStendNoRead`) и кнопки «Старт»/«Стоп» — в одной строке
(`stendButtonsLayout`), чтобы сразу было видно, какой режим использован при
старте. Группа зафиксирована `sizePolicy: Fixed×Maximum` (не растягивается
ни по ширине, ни по высоте сверх содержимого) + `alignment="Qt::AlignTop"`
у обоих элементов `stendTopLayout` — без этого панель растягивалась по
высоте соседней (правой) и центрировалась в строке, открывая пустоту сверху
и снизу.

**Правая панель «задал/измерил» (`groupCompare`/`cmpReport`) — текстовый
лог, не таблица и не график.** Заголовок группы убран, график
(`QCustomPlot plotStend`) и статус-лейбл (`lblStendStatus`) удалены —
активность стенда видна по цвету кнопки «Старт», отдельный индикатор не
нужен. Поле — `QPlainTextEdit name="cmpReport"`, readOnly,
`lineWrapMode=NoWrap` (иначе моноширинная таблица ломается переносом строк
при недостатке ширины — нужен горизонтальный скролл, а не перенос), шрифт
Consolas 10pt (отдельно от общего лог-шрифта 8pt у `logBrief`/`logView` —
иначе на глаз мельче рядом с обычными подписями), `minimumWidth=460`,
`minimumHeight=maximumHeight=480` (фиксированная высота независимо от
заполненности — иначе пустая/короткая панель не совпадала по высоте с
левой колонкой).

Формат записи на цикл (одинаковый и в живой панели, и в будущем файле
журнала):
```
             Стенд        Регистратор
Старт     14:31:00     14:31:00
Стоп      14:31:20     14:31:21
Интервал  00:00:20     00:00:21     +1 секунда
Общее     00:07:05     00:07:06     +1 секунда
Скорость  180          179          −1 об/мин
```
(метка переименована 02.07 из «Общая» в «Общее» — путалась с полем
«Наработка»/«Общее» отдельной панели «Память устройства», см. заметку в
шапке файла.)
Заголовок «Стенд/Регистратор» — один раз сверху, не повторяется в каждой
строке (раньше повторялся словами «стенд»/«рег.» в каждой строке —
признано избыточным). Время — чч:мм:сс, без долей секунды (касается и
Старт/Стоп, и Интервал/Общее — единый формат). Δ — только число со знаком
и полным словом единицы (секунда/секунды/секунд по русским правилам
числительных, об/мин), без отдельного значка «Δ». Δ есть у «Интервал»,
«Общее» и «Скорость» — в этом и смысл сравнения; у «Старт»/«Стоп» Δ не
считается (это абсолютное время, не разница). Блоки циклов разделены
пустой строкой — без неё все строки визуально слипаются. Панель держит
последние 4 цикла (подбирали по факту вместимости панели, было 2→3→4).

**Нижняя секция «Журнал испытания» (`groupTestJournal`/`tblTestJournal`)
убрана из `mainwindow.ui` целиком**, вместе с кнопками «Экспорт отчёта…»/
«Очистить», которые в ней были. Полная история теста не дублируется на
экране (это повторяло бы правую панель) — вместо этого пишется прямо в
файл на диске: при «Старт» файл создаётся, после каждого завершённого
цикла дозаписывается/флешится, чтобы авария/зависание программы не теряло
уже отработанные циклы. Смысл кнопок «Экспорт отчёта…»/«Очистить» (открыть
файл отчёта, начать новый файл) пока никуда не перенесён — отдельная
задача на этапе реализации файловой записи.

**Архитектура заполнения записи — частичная, не атомарная.** Сторона
«Стенд» в строке известна сразу (ПК сам отправил команды и засёк время).
Сторона «Регистратор» — не сразу: либо приходит с небольшой задержкой (на
связи), либо только постфактум при отдельном совмещении (автономный
регистратор). Технически: не редактировать конкретную строку текста в
`QPlainTextEdit` на месте, а держать историю циклов как структуру данных
(список записей с опциональными полями ПК/регистратор) и перерисовывать
видимый текст (`setPlainText`) целиком при каждом изменении.

**Два сценария совмещения с регистратором — оба сводятся к одной операции
«сопоставить запись стенда с записью регистратора по времени», просто
запускаемой в разный момент:**
1. *На связи* — регистратор в сервисном режиме, физически подключён через
   Nucleo (ретранслятор, адрес 0x8D, ничего в прошивке стенда менять не
   нужно — она уже прозрачно ретранслирует чужие пакеты). После каждого
   «Стоп» LOGLSMW сам пытается прочитать журнал регистратора (обычные
   таймаут/ретраи `DeviceController`) и дозаполняет строку почти сразу.
2. *Два разных лога* — регистратор работал автономно от батарейки без
   связи во время теста (или физически отвечает, но другая/несовместимая
   прошивка — поэтому решили не делать автоопределение по принципу «есть
   ответ/нет ответа», а явный чекбокс). Тогда колонка «Регистратор»
   остаётся пустой весь тест, а совмещение — отдельное ручное действие
   позже (даже в другой сессии программы): прочитать архив регистратора
   (тот же разбор, что уже умеет вкладка «Данные» — отдельный механизм
   чтения не нужен) и сопоставить с уже сохранённым файлом журнала стенда
   по времени. Кнопка такого совмещения — на вкладке «Стенд», не на
   «Данные» (у обычного пользователя «Данные» нет и не будет дампа стенда
   для сравнения, его результат по сути и есть правая колонка «Регистратор»).
Чекбокс `chkStendNoRead` явно выключает попытку чтения после каждого
«Стоп» — сценарий 2 выбирается вручную, не автоопределением.

**Три типа данных, не два:** Стенд / Регистратор (режим 1, описано выше) /
Logger (режим 2, потоковая запись по 256-байтным фреймам). Всё совмещение,
спроектированное в этом разделе, — только про Регистратор. Для
Logger-режима устройства стенд во время теста ведёт только свой собственный
журнал, без попытки опроса вообще — Logger не структурирован по циклам
вращения, опрашивать после каждого «Стоп» нечего. Проверка временной
точности для Logger возможна только полностью постфактум: прочитать дамп
Logger и журнал стенда раздельно, затем сопоставить (по сути — найти в
потоке сырых отсчётов IMU моменты начала/конца вращения и сверить с тем,
что скомандовал стенд). **Алгоритм поиска границ цикла в сыром потоке не
спроектирован — отдельная задача на будущее.**

**«Симуляция» (`chkSimulation`, верхняя панель подключения, рядом с
индикатором «● Регистратор») — общий сервисный флажок, не привязан только
к стенду.** Виден только в режиме «Сервис» (`setServiceMode`). При
включении заполняет `cmpReport` тестовыми данными в финальном формате —
чтобы оценивать вёрстку без подключённого железа. **Важно (уточнено
21.06.2026): это чисто визуальная заглушка, не имитация работы устройства
или процесса испытания** — задача только показать, как нынешний интерфейс
будет выглядеть с данными, никакой более точной симуляции (поведения
прибора, протокола, циклограммы и т.п.) не требуется и не планируется.
Это узкая черновая заготовка под одну конкретную вкладку, не полноценный
режим демонстрации программы без устройства целиком (тот более крупный
замысел — см. «Идеи
на будущее» ниже, отдельная нерешённая задача).

### Алгоритм циклограммы и совмещения журналов (сессия 21.06.2026)

**Построение ступенек скорости (решено).** N (количество ступенек) берётся
из «Смен скоростей» Мин/Макс (для режимов «Случайно ±»/«Только +»/«Только
−») или напрямую равно количеству уровней обхода в «N старт/стоп». Сами N
ступенек строятся от минимальной до максимальной скорости **включительно**:
первая ступенька = Мин, последняя = Макс, шаг = (Макс−Мин)/(N−1) (при N=1 —
одно значение, Мин=Макс). Длительность фазы «Работа» D берётся случайно из
Мин/Макс «Время работы», делится на N — это и есть время одной ступеньки;
по истечении этого времени берётся следующая ступенька.

**Режимы выбора следующей ступеньки (решено):**
- «Случайно ±» — случайный выбор из тех же N готовых ступенек (с повторами,
  это нормально, не баг — так ступеньки распределены равномернее, чем при
  выборе произвольного числа в диапазоне).
- «Только +» — последовательно по возрастанию через все N ступенек.
- «Только −» — последовательно по убыванию через все N ступенек.

**«N старт/стоп» — назначение и механика (важное уточнение 21.06.2026):**
это не сравнение точности времени, а **калибровка порога пробуждения
регистратора**. Стенд проходит ступеньки от Мин к Макс (та же формула
построения ступенек), и на **каждой** ступеньке делает полный старт/стоп
(а не смену скорости без остановки, как в трёх режимах выше), проверяя,
отозвался регистратор или нет. Не отозвался на скорости 5 — нормально,
идём на следующую ступеньку (6, 7…); как только отозвался — это и есть
минимальная скорость пробуждения регистратора, можно фиксировать и
переходить к оценке. Длительность ступеньки в этом режиме — та же логика
Мин/Макс «Время работы»/«Время простоя», отдельного поля не нужно (если
нужно быстро — просто ставится маленькое значение там же). Лог — тот же
формат, что и в остальных режимах, колонка «Регистратор» у нижних ступенек
закономерно пустая, пока порог не пройден. Применяется редко, разовая
калибровка — для него **не нужна** аккуратная долговременная логика
совмещения журналов (см. ниже), это не основной сценарий использования
стенда.

**Основной сценарий использования (уточнено 21.06.2026):** длительные
прогоны на «Случайно ±»/«Только +»/«Только −» — постановка на несколько
дней, на неделю. Чем дольше прогон, тем больше может «убежать» дрейф часов
между ПК/стендом и внутренними часами регистратора — отсюда и важность
именно поля «Общая» (накопленная длительность) и её Δ, а не только Δ
отдельного интервала.

**Совмещение записи стенда с записью регистратора по времени (решено,
заменяет более раннюю идею «допуск ±N секунд»):** не допуск и не поиск
ближайшего соседа для каждой записи отдельно — достаточно найти **одну
точку совмещения в начале** (первая реально совпадающая пара), а дальше
идти по обоим логам параллельно по порядку (запись N стенда ↔ запись N
регистратора со сдвигом, найденным в начале), без повторного поиска на
каждом шаге. При необходимости (если сильно разойдётся) можно будет
сделать повторную точку совмещения где-то дальше — вручную, отдельная
задача, не автоматика.

**Пропуски записей у регистратора — известный, но редкий случай.** В
реальной практике бывали случаи, когда регистратор не записывал какой-то
интервал вовсе (пропуск). Раньше при ручном сведении это означало вставку/
вырезание строки — муторно, но решили: раз это редкость, а главный интерес
при длительном прогоне — агрегированная «Общая» Δ по всему тесту, а не
идеальное совпадение каждой отдельной строки, отдельную автоматическую
логику обнаружения пропуска **специально не строим**. Совпадение строк
по отдельности важно скорее как диагностика (искать, в какой момент начал
копиться разбег, когда что-то пошло не так) — для уже отлаженного,
проверенного устройства это не критично.

**Формат файла журнала на диске (решено, закрывает последний открытый
вопрос):** тот же читаемый текстовый блочный формат, что и в `cmpReport`
(не CSV/таблица) — рендерим туда то же самое. Обоснование: совпадение
строк нужно прежде всего для ручного разбора глазами (искать место
разбега), для этого читаемый текст удобнее таблицы; машинный анализ (если
когда-то понадобится) к формату файла не привязан. **Это решение — только
для Регистратора.** Для Logger-режима (см. «Три типа данных» выше) объём
данных принципиально другой («куча данных», которые потом нужно будет
обрабатывать программно, не просматривать глазами построчно) — там
**решено (21.06.2026): CSV**, не читаемый текстовый блок (минимум кода для
записи из Qt через `QTextStream`, открывается в Excel/Python для анализа).
Точная раскладка столбцов CSV **не выбрана** — естественно откладывается
вместе с самим алгоритмом поиска границ цикла (см. выше): структуру
столбцов разумно проектировать вместе с алгоритмом, а не раньше него.

---

## Мониторинг (вкладка «Мониторинг») — реализован 22.06.2026

**Назначение, под которое делался первый проход:** проверить стенд «в
лоб» — пока крутится мотор (вкладка «Стенд»), читать гироскоп
регистратора и сверять измеренную угловую скорость (°/с) с заданной
стендом (об/мин × 6 = °/с). До этого вкладка была чистым UI-каркасом без
какой-либо логики («этап 4» в комментарии `setupDashboardPlots()`) — ни
один чекбокс/кнопка не были подключены.

**Что сделано:** `setupMonitor()`/`MonState` (`mainwindow.h/.cpp`) —
периодический опрос по таймеру (`m_monTimer`, период из `cmbPollPeriod`,
50–5000 мс): `GET_AXES_RAW` (Acc+Gyro одной командой, если включён хотя бы
один из чекбоксов Acc/Gyro) и до 3 температур (`GET_TEMP_IMU/TMP117/STM`,
по своим чекбоксам `chkTLsm/chkTTmp/chkTStm`). Графики `plotAcc`/`plotGyro`
(по 3 линии X/Y/Z с легендой) и `plotTemp` (3 линии LSM/TMP117/STM32) —
скользящее окно (`cmbTimeWindow`, 10–300 с), старые точки обрезаются по
cutoff в `monTrim()`. «Старт» очищает буферы и ось времени
(`QElapsedTimer m_mon.t0`), «Пауза» — переключатель без сброса буфера,
«Стоп» — останавливает таймер, не трогая отображённые данные, «Очистить»
— сбрасывает буферы (и ось времени, если мониторинг ещё идёт) в любой
момент. «CSV…» — `QFileDialog::getSaveFileName` + `QTextStream`, формат
длинный (`t_s,channel,value`), т.к. у каналов разные моменты опроса (не
синхронны) — выравнивать их в широкий формат не стали, не было нужно для
первой версии.

**Перевод raw LSB → физические единицы.** Прошивка (`com.c`) уже умеет
`ACC_GET_FS`/`GYRO_GET_FS` (0x17/0x19 — реализованы, в отличие от
`*_SET_ODR`/`*_GET_ODR` 0x12/0x14/0x16/0x18, которые в прошивке заглушки
`er_not_impl`) — LOGLSMW читает факт. полную шкалу IMU **один раз при
«Старт»** и переводит raw int16 в g/°/с по таблице чувствительности ST
(`accSensitivityMg`/`gyroSensitivityMdps` в `mainwindow.cpp`, значения из
`Firmware/LOGLSMA/App/Inc/lsm6dso.h`). Дефолт до первого ответа (и
fallback при неожиданном значении FS) — ±2g/±2000dps, это ровно то, что
ставит `LSM6DSO_Init()` при включении IMU.

**Сознательно не сделано в этом проходе (см. «начнём со штатных условий»,
22.06.2026):** конфигурация датчика (`ACC_SET_FS`/`GYRO_SET_FS` — команды
существуют и работают в прошивке, но в UI «Мониторинга» не вызываются) —
опрос идёт на текущих, уже установленных настройках IMU, ничего не
переключаем программно. `ACC_SET_ODR`/`GYRO_SET_ODR` (частота опроса
самого IMU, не путать с периодом опроса по UART) в прошивке не реализованы
вообще — даже если понадобится, начинать пришлось бы с прошивки.

**Открытый вопрос (не для этого прохода):** какая ось гироскопа (X/Y/Z)
физически соответствует оси вращения стенда — зависит от того, как
регистратор закреплён на стенде, программно не определяется, нужно смотреть
глазами, какая из 3 линий `plotGyro` отзывается на вращение.

---

## Firmware — правила работы с кодом

- **Весь пользовательский код** — только в `App/Src/` и `App/Inc/`
- `Core/` — генерируется CubeMX, не редактировать вручную
- В файлах Core/ только между метками `/* USER CODE BEGIN */` / `/* USER CODE END */`
- Новые `.c` файлы из App/ добавлять в `CMakeLists.txt` → `target_sources`
- Toolchain: **CMake** (не Makefile)
- RTOS: **нет** (голый HAL) — FreeRTOS несовместим с deep sleep режимами

### ⚠️ Регенерация .ioc — известный класс потерь (02.07.2026)

Одна из регенераций LOGLSMA из CubeMX молча потеряла СРАЗУ НЕСКОЛЬКО
вещей, что дало «команды не проходят» на ровном месте (разбор —
`session_notes_2026-07-02_passive_wkup1_bug.md`):
- `HAL_GPIO_Init` для PA0/PC13 из `gpio.c` (CubeMX для роли `SYS_WKUPx`
  init не генерирует вообще) — теперь оба инициализируются в `main.c`,
  `USER CODE BEGIN 2`;
- `USART2.BaudRate` откатился 921600→115200 (`usart.c` и `.ioc` — .ioc,
  видимо, регенерировался из версии до правки 22.06);
- USER CODE блок USART1 (BLE) в `HAL_UART_MspInit`/`MspDeInit`
  (`usart.c`) — USART1 в `.ioc` не сконфигурирован, CubeMX выкинул.

**После КАЖДОЙ регенерации `.ioc` — обязательный `git diff` по
`Core/Src/` и `.ioc`**: искать не только новые строки, но и ПРОПАВШИЕ
(инициализации пинов, скорости UART, USER CODE блоки, добавленные
вручную под несконфигурированную в .ioc периферию).

### ⚠️ Сборка LOGLSMA в CubeIDE: ВСЕГДА Clean, потом Build/Run (13.07.2026)

Инкрементальный Build (`ninja`) иногда молча НЕ пересобирает изменённый
`Data.c`, хотя контент на диске свежий (`ninja: no work to do.` при реально
правленном файле — конкретный случай разобран
`session_notes_2026-07-13_v2_record_vibration.md` §3/§7): версия прошивки
(индикатор из `com.c`) при этом ОБНОВЛЯЕТСЯ (этот файл пересобрался), а
поведение датчика — нет (старый `Data.c.obj` из кэша), что маскируется под
«баг в коде», хотя баг в сборке. **Правило: перед прошивкой правки в
`App/Src/*.c` — сначала `Project → Clean...` (выбрать именно LOGLSMA,
«Build only the selected projects»), и только после чистой пересборки —
Run/Debug на прошивку.** Индикатор версии подтверждает свежесть ТОЛЬКО
`com.c` — не гарантия, что пересобрались остальные файлы.

### ⚠️ Сборка/прошивка LOGLSMA в VS Code — РАБОЧИЙ РЕГЛАМЕНT (17.07.2026)

Переехали на VS Code (cortex-debug + ST-Link, CMake-preset **Debug**, Ninja,
gcc-arm; ELF `build/Debug/LOGLSMA.elf`). Разобран НАСТОЯЩИЙ корень многодневной
эпопеи «шьётся старый Data.c», подтверждён на железе:

1. **Правки Claude меняют СОДЕРЖИМОЕ файла, но НЕ обновляют mtime на стороне
   Windows** (даже `touch` из среды Claude до Windows-mtime не долетает). Ninja
   сравнивает mtime → «`ninja: no work to do.`» за 0.06 с → в ELF остаётся
   старый объектник. «Код выхода 0» при этом ОБМАНЧИВ — ничего не собралось.
2. **F5 (cortex-debug) сам НЕ компилирует** — заливает тот ELF, что лежит.
   Прошивка без предварительной сборки = гарантированно старый образ.

**РЕШЕНИЕ (внедрено, работает):**
- `.vscode/tasks.json` — задача `CMake build (Debug)` c `"command":
  "cleanRebuild"` (ПОЛНАЯ пересборка, mtime-независимо).
- `.vscode/launch.json` — `"preLaunchTask": "CMake build (Debug)"` → **каждый
  F5 сперва делает clean rebuild, потом шьёт.** Цена — +~30–60 с на прошивку,
  зато «свежее/несвежее» уходит из головы совсем.

**ПРАВИЛА:**
- Claude после КАЖДОЙ правки прошивки: (а) бампает версию `FW_YY..MI` в `com.c`
  на текущее время; (б) `touch` файла (не повредит, хоть Windows-mtime и не
  ловит).
- Свежесть проверяем ТОЛЬКО индикатором версии в LOGLSMW → «Данные». Совпало с
  тем, что Claude поставил → в контроллере именно эта правка. НЕ совпало → сборка
  не прошла (частый случай: `no work to do`) — форсировать `CMake: Clean
  Rebuild`, перешить.
- **Отладчиком свежесть НЕ проверяем.** Эта прошивка уходит в Stop2 (сон) → SWD
  теряет ядро → `Target is not responding / Lost target connection` СРАЗУ после
  `File download complete`. Это НОРМА, не крах: заливка успела до сна. Судим по
  версии в LOGLSMW, а не по отладчику.
- Признак верной сборки в терминале «Сборка»: идут строки `Building …
  Data.c.obj`/`com.c.obj` и ~полминуты, а НЕ `no work to do` за доли секунды.

### ⚠️ ВЕРСИИ ОБЕИХ ПРОГРАММ — держать актуальными (A и W, 17.07.2026)

С 17.07 версионируются **обе** программы; Claude обязан поднимать версию той,
которую правит (это основные, почти рабочие версии):

- **A — прошивка LOGLSMA.** Версия — 5 `#define FW_YY/MM/DD/HH/MI` в
  `Firmware/LOGLSMA/App/Src/com.c` (формат ГГ.ММ.ДД ЧЧ:ММ). Правил код прошивки →
  ставлю текущее время. Виден в LOGLSMW → «Данные» → «Версия ПО». **Инвариант:**
  версия на «Данные» ДОЛЖНА совпадать с тем, что поставил Claude; расхождение =
  стейл-сборка (см. регламент VS Code выше), стоп-сигнал.
- **W — приложение LOGLSMW.** Версия — `#define LOGLSMW_VERSION` в
  `SoftWare/LOGLSMW/main.cpp` (формат ГГ.ММ.ДД.ЧЧ.ММ). Правил приложение → поднимаю.
  Виден в **заголовке окна / панели задач**: `LogLSMW · ГГ.ММ.ДД.ЧЧ.ММ`
  (`LOGLSMW_BRAND` = дизайнерское имя, техническое `LOGLSMW` в QSettings НЕ менять).
  Иконка окна — `SoftWare/LOGLSMW/logo.png`/`.ico` (грузится в рантайме по
  `LOGLSMW_SRC_DIR`, фолбэк — монограмма «LS»).

Правило коротко: **тронул прошивку — бампни A; тронул приложение — бампни W.**
Не менялась — не трогаю (у A совпадение версии = признак свежести сборки).

### Стиль кода
- Стандарт: C11, snake_case, UPPER_CASE для макросов
- Префиксы: `ltp_`, `lsm6dso_`, `flash_`, `log_`, `rtc_`, `power_`
- HAL: всегда проверять возврат `HAL_StatusTypeDef`, не использовать `HAL_MAX_DELAY`
- ISR: минимум кода, флаг + обработка в main loop

---

## Режимы работы

| Режим | Источник питания | Активность |
|---|---|---|
| PASSIVE | Батарея LS14250 (3.6В) | Глубокий сон, IMU low-power. **Цель — Standby 280 нА с RTC** (см. «Сон» ниже) |
| WORK | Батарея | Сбор данных, запись в Flash |
| SERVICE | Внешнее (+VEX / +5VU) | UART/USB активен, все команды доступны |

Переход PASSIVE→SERVICE: WKUP1 (PA0) — детект внешнего питания.
Переход PASSIVE→WORK: WKUP2 (PC13) — INT1 от IMU (движение).

### ⚠️ Сон в «Работе»: Standby, НЕ Stop2 (не путать — согласовано, фиксировано)

Глубокий сон рабочего цикла — **STANDBY 280 нА с RTC**, а НЕ Stop2. Stop2
(1.28 мкА) — только средний ярус для коротких внутрицикловых пауз. Двухъярусно:

- **Долгий простой** (большой лаг бездействия) → **Standby 280 нА** (с RTC).
  Выход из Standby = **сброс МК** (ОЗУ теряется — заложено: stub во Flash +
  дозапись, теряется максимум 1 незакрытый цикл); восстановление —
  backup-регистры + Flash-stub (перенос `reset.c`-механики из rp_device —
  TODO). Из Standby будит **только WKUP-пин / RTC / сброс** (EXTI не работает)
  — поэтому PA0/PC13 в `.ioc` заведены как **SYS_WKUP1/SYS_WKUP2**, это
  единственно верно, EXTI на них не заводить.
- **Короткие паузы/перепроверки внутри рабочего эпизода** → Stop2 (контекст
  жив, ОЗУ сохраняется).

**Текущая прошивка = только Stop2 (средний ярус).** Standby-ярус (вход по
таймауту бездействия, восстановление по backup/stub) — ещё не реализован
(TODO). Целевое значение источника цитируется по даташиту: 280 нА Standby с
RTC против 1.0/1.28 мкА Stop2 — экономия существенная, поэтому цель именно
Standby.

**Тест vs Работа (два вида, две команды — не путать):**

- **Работа** (`CMD_START_REGISTER` 0x1D) — рабочая программа СО сном: автомат
  SLEEP→CONFIRM→ROTATING, в SLEEP уходит в сон (цель Standby, сейчас Stop2),
  связь с ПК на время сна теряется, наружу — только `CMD_CYCLE_PUSH`.
- **Тест** (`CMD_START_TEST` 0x23, новая) — та же рабочая программа БЕЗ сна:
  контроллер всё время на связи, в SLEEP-фазе не спит, а бодро крутится с
  `ComPoll()` и опросом флага `WUF2` (тот же фронт INT1/WKUP2, что будит из
  сна в «Работе», но здесь ловится флагом на ходу — видно, что прерывание
  пришло). Всё остальное общее с «Работой»: детект → CONFIRM → ROTATING →
  анализ цикла → запись во Flash → пуш. Стоп для обоих — `CMD_STOP_REGISTER`
  (0x22).

**WKUP2 (PC13) жёстко разведён только на INT1 LSM6DSO (подтверждено
22.06.2026) — другого назначения у этого пина нет.** Это важно для отладки
на стенде с тумблерами VDD/WAKE (см. ниже): тумблер «WAKE» не может
управлять переходом в SERVICE, даже если кажется логичным по названию —
SERVICE зависит только от WKUP1 (PA0), а WAKE на стенде, скорее всего,
относится к WKUP2/имитации движения, либо вообще к другой цепи.

**Загадка 22.06 «PA0 не видит HIGH ни при одной комбинации тумблеров» —
разгадана и закрыта 02.07.2026** (см.
`session_notes_2026-07-02_passive_wkup1_bug.md`): дело было не в цепи
платы, а в том, что `HAL_GPIO_Init` для PA0 молча выпал из `gpio.c` при
регенерации `.ioc` — пин оставался в сбросовом Analog-режиме, где у
STM32L4 входной триггер Шмитта аппаратно отключён и `HAL_GPIO_ReadPin`
недостоверен (почти всегда 0). Пробуждение из Stop2 по WKUP1 при этом
работало (у PWR свой тракт детекта, не через GPIO-триггер) — это и
запутывало. Фикс: явный `HAL_GPIO_Init` (Input/NOPULL) для PA0 И PC13 в
`main.c`, `USER CODE BEGIN 2` (в `gpio.c` нельзя — там USER CODE секция
вне тела функции). Подтверждено на железе: тумблер питания реально
управляет входом в Service().

**Обход `if (1 || ...)` снят (02.07.2026).** Архитектура входа —
финальная, согласована, не переоткрывать:
- **Режим B (SERVICE)** — вход ТОЛЬКО по текущему УРОВНЮ WKUP1
  (`wkup1_pin_set()`, кабель/внешнее питание есть = сидим в `Service()`),
  без фронта/флага WUF1.
- **Режим A (WORK, автомат SLEEP→CONFIRM→ROTATING,
  `RotationStateStep()`/com.c, вызывается только из main.c)** — реальный
  Stop2; запускается ТОЛЬКО командой `CMD_START_REGISTER` (0x1D) →
  `regist.monitoringActive=1`; после запуска от УРОВНЯ WKUP1 не зависит.
  `PushCycleRecord()` в конце цикла сама смотрит уровень WKUP1: кабель
  есть — дублирует запись по LTP (CMD_CYCLE_PUSH), нет — молчит.
- **Гейт входа в Service (03.07.2026, найдено при первой проверке
  стенда):** верхняя проверка main.c — `if (!monitoringActive &&
  wkup1_pin_set())`. Без гейта запущенный автомат при подключённом кабеле
  (на стенде — всегда, через Nucleo) немедленно затягивался обратно в
  Service() и не делал ни одного шага — вкладка «Стенд» не получала ни
  одного CMD_CYCLE_PUSH («вкладка перестала работать» после перестройки
  A/B 02.07).
- **Остановка режима A (03.07.2026, согласовано):** основной путь —
  `CMD_STOP_REGISTER` (0x22, `cmdStopRegister()`/com.c): в бодрых фазах
  автомата (CONFIRM/ROTATING) main.c попутно зовёт `ComPoll()` (только
  при кабеле) — команда доходит, идущий цикл закрывается штатно
  (`RotationFinishCycle()` — общий помощник с дебаунс-веткой), затем по
  верхней проверке — Service(). Дополнительный путь — ФРОНТ WKUP1 во
  время Stop2 (тумблер на линии, если есть в конкретной сборке стенда —
  фишка, не основной механизм; подключение кабеля к спящему автономному
  устройству даёт тот же фронт → выход в Service, это осмысленно). В
  SLEEP (Stop2) LTP недостижим по определению: команды в паузах теста
  таймаутят — штатно, не баг.
- Флаги WUF1/WUF2 в Stop2-ветке разбираются оба, симметрично — это
  механизм ВЫХОДА из Stop2 (пока МК спит, уровень читать некому), не
  решение о входе в Service(); решение — отдельно, по уровню.
- **Стендовый сценарий (LOGLSMW, 03.07.2026):** «Старт» циклограммы шлёт
  0x1D ПЕРВЫМ (до команд мотору — автомат должен уже ждать вращение),
  «Стоп» — 0x22 сразу после STEND_STOP (мотор тормозит по рампе →
  регистратор ещё в ROTATING и слышит LTP). Во время теста LOGLSMW НЕ
  опрашивает 0x8D (гейты по `m_stendActive`: опрос часов/температур
  `m_devClockTimer` — добавлен 03.07, двоичный поиск Flash — был с
  02.07) — связь только входящими CYCLE_PUSH. Таймаут 0x22 (стоп нажат
  в паузе — регистратор спит) — подсказка в журнале: тумблер/питание,
  либо повторный краткий прогон.
- ⚠️ Весь стендовый сценарий 0x1D/0x22 и Stop2 (ток, пробуждение по
  WKUP2, калибровка порога Wake-Up датчика) на железе после правок 03.07
  ещё не проверялись — проверка на стенде в работе.

---

## Управление питанием периферии (вариант A)

| GPIO | Label | ON | OFF | Что управляет |
|---|---|---|---|---|
| PA8 | QPWR | Output HIGH | **Analog (Hi-Z)** | Flash P25Q128H |
| PB4 | T_PWR | Output HIGH | **Analog (Hi-Z)** | TMP117 |
| PB5 | M_PWR | Output HIGH | **Analog (Hi-Z)** | FRAM |
| PB6 | B_PWON | Output LOW | **Analog (Hi-Z)** или Output HIGH | BLE (BSS215P через P-канальный транзистор) |

**⚠️ OFF = Analog (Hi-Z), не Output-LOW.**  
Output-LOW — активный сток: конденсаторы периферии разряжаются через MCU-вывод.  
Analog — вывод Hi-Z, нода питания держится внешними резисторами без участия MCU.

B_PWON: делитель R1/R2 на VDD + P-канальный управляющий транзистор перед затвором BSS215P.  
- MCU LOW → P-канальный транзистор открыт → затвор BSS215P притянут к GND → BSS215P ON → BLE питается  
- MCU HIGH/Hi-Z → транзистор закрыт → R1 держит затвор BSS215P у VDD → BSS215P OFF → BLE обесточен  
Для B_PWON оба OFF-варианта приемлемы: Hi-Z и HIGH дают один результат.

При старте всё обесточено. Включать только нужное для текущего режима.

---

## Организация Flash P25Q128H

- 0x000000–0x0000FF: заголовок (версия, режим, указатель записи, флаги)
- 0x000100+: записи лога
- При заполнении запись прекращается, старые данные не перезаписываются
- Стирание только по команде из SERVICE режима

**Режим 1 (Регистратор):** запись 24 байта за цикл вращения, до 10 записей
на страницу Flash (пересмотрено 20.06.2026 — было 1 запись = 1 страница)  
**Режим 2 (Logger):** потоковая запись фреймов по 256 байт (1 фрейм = 1
страница), внутри — сэмплы Ax/Ay/Az/Gx/Gy/Gz по 12 байт  
Спека форматов: `actual/data_format_spec_v1.md`

**`SaveParamOnEEPROM` реально пишет во Flash (реализовано 02.07.2026)** —
раньше была заглушка (`return HAL_OK` без действий, данные никогда не
попадали на чип). Раскладка записи (24 байта): tsStart(u32) | duration(u32)
| durationTotal(u32) | maxVibro(float, пока всегда 0.0 — акселерометр не
подключён к подсчёту) | maxRpm(float) | reserved(2) | CRC16-CCITT(2).
Курсор записи ищется линейным сканом страниц при первом обращении
(`flashFindWritePos`, читает первые 4 байта каждой страницы: 0xFFFFFFFF =
пусто) и кэшируется до перезагрузки МК. `LoadTotalSec` восстанавливает
lifetime `totalSec` из последней записи с проверкой CRC (откат на 0 при
несовпадении — старые записи без CRC или повреждение).

**Баг FLASH_READ (0x07) «не отвечает» — найден и исправлен (02.07.2026,
поздний вечер).** Долго казалось багом QSPI-драйвера/dummy-циклов Fast Read
(перебирались 10/8/4 — ни один не помог), пока диагностика сырыми LTP-
байтами напрямую по COM-порту (в обход LOGLSMW) не показала: ответ
корректен для size вплоть до 240, а с size≈250+ систематически обрывается
(приходит только «хвост», пропорционально размеру). LOGLSMW всегда
запрашивает целую страницу (256 байт) — отсюда стабильный «нет ответа»,
маскировавшийся под баг чтения Flash. **Настоящая причина**: `sendPacket`/
`sendError` (`com.c`) слали весь ответ одним блокирующим
`HAL_UART_Transmit()`; на 921600 бод без аппаратного flow control
(`HwFlowCtl=NONE`) внутренний буфер USB-UART моста не успевает сливать
данные по USB при непрерывном куске длиннее ~250 байт и роняет середину
потока. **Исправлено**: новая `UartTransmitChunked()` шлёт ответ кусками по
64 байта с паузой `HAL_Delay(2)` между ними — протокол LTP не менялся,
правки на стороне LOGLSMW не понадобилось. Подтверждено на живом железе
(«Тест памяти» → «Чтение» реально читает страницы). dummy-циклы Fast Read
в итоге оставлены на исходном значении 10 (QPI-режим, `p25q128.c`) —
подтверждённо рабочее значение (сверено с `rp_device` и историческим
тестированием записи/чтения на том же чипе), к самому багу отношения не
имели. Подробности перебора и диагностики —
`actual/session_notes_2026-07-02_flash_and_reg_column.md`.

---

## Актуальная документация

| Файл | Версия | Статус |
|---|---|---|
| `md/LogLSM_TZ_v4_consolidated.docx` | v4 | ✅ актуален |
| `actual/A+E1_pinout_from_schematic.md` | — | ✅ верифицирован нетлистом |
| `actual/B_pinout_from_schematic.md` | — | ✅ верифицирован нетлистом |
| `actual/cubemx_config_LOGLSMA+MA00.md` | — | ✅ готов к использованию |
| `actual/data_format_spec_v1.md` | v1 | ✅ финальный |
| `Doc/LogLSM Transport Protocol/TMP/LTP_PROTOCOL.md` | draft v1 | ✅ транспорт готов |

---

## Термотест (вкладка «Тест памяти») — алгоритм (17.07.2026)

Проверка работоспособности NOR-Flash при высокой/низкой температуре. Смысл —
не разовая большая запись, а **покрытие**: прогонять блок страниц через много
температурных точек. Реализация — `tempRun*` в `mainwindow.cpp`.

**Ключевой инвариант (зафиксирован по требованию пользователя):**
- **Температура ≥ МАКС** → операции (запись/сверка) **ПРЕКРАЩАЮТСЯ**, пока не
  опустится ниже МАКС. Симметрично **≤ МИН** → пауза, пока не поднимется выше
  МИН. (`outOfRange` в `tempRunHandleTemp`.)
- В рабочем диапазоне [МИН..МАКС] операция запускается на **каждом изменении на
  ШАГ °C — и на РОСТЕ (нагрев), и на ОСТЫВАНИИ** (`|Δt| ≥ ШАГ`, обе стороны).
- Индикация активности на кнопке: **🌡** — идут операции; **⏳** — пауза (вне
  диапазона, ждём возврата температуры). Кнопка во время прогона = «Стоп».

**Параметры (все из вкладки «Тест памяти»):**
- Датчик — радиокнопки **TMP117 / IMU(LSM) / STM32** (`tempRunTempCmd`); имя
  пишется в лог старта.
- **МИН / МАКС / ШАГ °C** — рабочий диапазон и температурный шаг (группа «Термотест»).
- **«проходов»** (спинбокс рядом с «ШАГ °C») — сколько страниц пишется/сверяется
  на КАЖДОЙ температурной точке. Блок = [«Старт», «Старт»+проходов), гранулярность
  — страница (запись байта → сразу сверка этой же страницы). НЕ путать с «Страниц»
  (то — для байт-теста Запись/Чтение).
- **Байт** («Задать: Байт») — шаблон записи; фиксируется на прогон. (Был баг:
  писал захардкоженный 0xA5 → убрано.)
- **«Ошибок, байт»** — счётчик НЕкорректных БАЙТ (не страниц).
- Живой вывод: гекс-панель обновляется по мере записи блока (рендер напрямую из
  сверяемых страниц; `memTestDump` во время термотеста звать нельзя — конфликт
  машины состояний).

---

## LOGLSMW — соглашения UI (`SoftWare/LOGLSMW/`)

- **Пустое/неизвестное значение в индикаторах — пробел, не прочерк.** Везде,
  где QLabel показывает «факт»/индикатор без значения (нет связи, нет
  данных, образ не загружен) — текст-плейсхолдер `" "` (пробел), **не**
  `"—"` (решение 20.06.2026). Касается любых новых индикаторов на вкладках
  LOGLSMW.
- **«Факт»-ячейки (индикаторы)** — это `QLabel` с QSS-стилем (рамка
  `border`, скругление `border-radius`, серый фон `background: #F5F4F0`,
  `padding`) — список селекторов в `mainwindow.cpp`,
  `QPushButton::setStyleSheet` ближе к концу `applyStyles()`/настройки
  стилей. Любой новый «факт»-индикатор должен попасть в этот же список
  селекторов, иначе будет выглядеть как голый текст без рамки.
- В сетке параметров (`memParamsGrid`, вкладка «Тест памяти») каждая строка
  — 3 ячейки: подпись | бокс | бокс (даже если у строки нет настоящего
  «Задать» — тогда левый бокс — статичный пробел, а не растянутая на оба
  столбца ячейка).

### Известные ограничения (TODO)

- **«Прочитать» не видит реальные данные устройства, привезённого из поля.**
  Кнопка «Прочитать» (вкладка «Тест памяти») берёт диапазон страниц из
  `lblImgAddr`/`lblImgPages` — а они заполняются ТОЛЬКО когда образ был
  записан из самого LOGLSMW в текущей сессии (`FLASH_WRITE`, тег `TagImg`,
  см. `memTestDump()` в `mainwindow.cpp`). Если на чипе реальные данные с
  устройства (не наша тестовая запись) — эти 2 строки пустые, и «Прочитать»
  отвечает «Нет записанного образа…», хотя данные на чипе есть. Замечено
  20.06.2026. Для чтения устройства с реальными (полевыми) данными нужен отдельный путь к
  диапазону — вероятно через уже существующий `probeFlashImageState()` /
  разбор архива (они умеют находить границы журнала на чипе независимо от
  сессии записи) — **не реализовано**.

  **Уточнение приоритета (20.06.2026):** «Прочитать» по сути должна иметь 2
  варианта — (1) после записи тестового образа читает именно тот диапазон,
  куда писала (сделано), (2) для устройства с реальными данными — читать весь
  записанный диапазон по его фактическим границам (не сделано, см. выше). Но
  вариант (2) во многом **дублирует** то, что уже умеет вкладка «Данные»
  (она читает и показывает сводку по устройствам с реальными данными уже сейчас,
  проверено на практике 20.06.2026). Реальная ценность варианта (2) — не
  «увидеть данные» (это уже есть), а именно **гекс-дамп по сырым байтам**
  для диагностики на уровне байт (то, чего сводка на «Данные» не показывает).
  Из-за дублирования с «Данные» — низкий приоритет, не блокирует ничего
  другого.

  **Аудитория (уточнено 20.06.2026):** гекс-дамп реальных данных интересен
  только разработчику/инженеру, не конечному пользователю — что совпадает с
  тем, что вкладка «Тест памяти» и так часть инженерного режима («Сервис»,
  см. `setServiceMode()`), не показывается в обычном режиме «Данные». То
  есть закрывать эту функцию от пользователя отдельно не требуется — она уже
  там, где нужно, по существующему разделению режимов.

  **UX на будущее, когда вариант (2) будет реализован (решено 20.06.2026):**
  дамп устройства с реальными данными может быть большим (до 65535 страниц) и идти
  заметно дольше, чем тестовый образ — кнопка «Прочитать» должна работать как
  переключатель: повторное нажатие во время идущего чтения останавливает его
  досрочно (не ждать до конца), с явной обратной связью на экране (текст
  кнопки/лог), а не тихая отмена. Такой же паттерн уже использован для
  «⌛ стирание…» при автостирании перед записью образа — переиспользовать его.

- **Паспорт устройства и защита реальных данных (план, обсуждено 20.06.2026,
  ПОЛИТИКА ПЕРЕСМОТРЕНА 19.07.2026, не реализовано).** У блока «Индивидуальная
  информация устройства» (вкладка «Тест памяти»: `editSerial`/Серийный №,
  Вариант, Дата выпуска) сейчас нет ничего за интерфейсом — поля статичные,
  нет LTP-команды ни на чтение, ни на запись (проверено: не упоминается ни в
  `mainwindow.cpp`, ни в `devicecontroller.h`, ни в прошивке `com.c`/`com.h`).
  Это предварительное условие для всего нижеследующего.
  - **ПАСПОРТ (серийный №) ЕСТЬ ВСЕГДА** у любого устройства — вопрос не
    «прописан/не прописан», а **активирован он или нет** (отдельный флаг +
    ts_activation, см. §11 заметки 18.07). Уточнение 19.07.2026 —
    отменяет прежнюю формулировку «инициализировано = серийник не пуст».
  - **Паспорт правится ТОЛЬКО по паролю в режиме «Сервис»** (не свободное
    редактирование поля) — это и есть защита от случайной/несанкционированной
    правки, ВМЕСТО прежней идеи «блокировать стирание у паспортизованного».
  - **Защита реальных данных — через АВТО-ДАМП, а не блокировку (решение
    19.07.2026):** если устройство АКТИВИРОВАНО, LOGLSMW при подключении
    **сам сохраняет дамп Flash** (образ журнала) на диск, с возможностью
    последующей загрузки/восстановления. То есть случайное стирание не
    фатально — есть свежая копия. Прежний план «жёстко блокировать кнопки
    стирания + сброс ID для разблокировки» — ОТМЕНЁН в пользу этой схемы
    (проще в работе, не мешает инженеру, данные всё равно защищены копией).
  - «Прочитать»/разбор архива (`probeFlashImageState()`) — механизм чтения
    реальных данных, переиспользуется и для авто-дампа при подключении.
  - **Только СЕРИЙНЫЕ (с паспортом) устройства могут быть АКТИВИРОВАНЫ**
    (уточнение 19.07). Цвета сегментов активации: операция → ЖЁЛТЫЙ; полная
    активация последовательно → ЗЕЛЁНЫЙ; сторож сработал заново → КРАСНЫЙ.
  - **СБРОСА ID НЕТ** — паспорт+калибровки = ЗАМОРОЖЕННЫЕ параметры. Правка —
    на «Калибровке» по **«ШЕФ»-паролю**. ДВА ПАРОЛЯ: (1) обычный → «Сервис»;
    (2) «шеф» → замороженные параметры + тоже пускает в «Сервис». «Стендовый»
    режим = просто подключён к сервисному разъёму (сброса нет).
  - **АВТО-ДАМП Flash на диск при ПЕРВОМ подключении после активации** (метка,
    исключает чел. фактор), для ОБОИХ режимов (Регистратор/Логгер). +
    предусмотреть режим LOGLSMW «отображение данных из файла» (просмотр дампа
    без устройства).
  - Зависимости по реализации: 1) LTP-команды GET/SET паспорта + флага
    активации + ts_activation (внутр. Flash STM32 стр.123, прошивка) — см.
    §11 заметки 18.07 (шаг 1); 2) два пароля (Сервис + шеф на «Калибровке»);
    3) авто-дамп Flash при 1-м подключении активированного + загрузка/просмотр
    дампа из файла. Без п.1 пункты 2–3 не имеют смысла.
  - **Связанная, но отдельная задача (замечено 20.06.2026):** паспорт сам по
    себе (несколько байт) пишется во внутреннюю Flash STM32 прямо из
    работающей прошивки (HAL_FLASH_Program по команде LTP в сервисном
    режиме) — самостоятельный загрузчик для этого не нужен. Но раз речь
    зашла о внутренней Flash, заодно зафиксировать: для обновления самой
    прошивки по сервисному UART (без ST-Link/SWD, в поле) понадобится свой
    загрузчик — отдельная, более крупная задача (отдельный режим работы +
    протокол приёма образа), не входит в реализацию паспорта и не зависит от
    неё технически, просто рядом по теме «работа с внутренней Flash по
    UART». Не спроектирован, не реализован.

---

## Идеи на будущее (нереализовано, не забыть)

- **Галочка «Симуляция» в сервисном меню LOGLSMW** (заметка 21.06.2026,
  без детальной проработки). Идея — переключатель где-то в «Сервис»,
  включающий режим работы без реального подключённого устройства (для
  отладки UI/сценариев без железа). Не определено: что именно эмулирует
  (ответы на LTP-команды, фейковый архив Flash, обе вкладки «Стенд» и
  «Тест памяти», или только часть) и как пересекается с уже существующей
  имитацией загрузки образа (см. задачу «Установка часов при имитации
  загрузки образа» — там уже есть какой-то прообраз симуляции для
  загрузки тестовых образов). Требует отдельного обсуждения перед
  реализацией.

- ~~«Образ: страниц»/«Образ: начало» — сделать индикаторами реальных
  границ Flash, не только памяти сессии»~~ — **реализовано 02.07.2026.**
  `flashBinSearchStart/SendNext/HandlePage/UpdateUi` (mainwindow.cpp/h) —
  двоичный поиск первой свободной страницы по `TZ_LOGLSMW_v2.md` §10,
  как и планировалось (заголовка-указателя во Flash по-прежнему нет —
  решение 12.06 не делать заголовок остаётся в силе). `lo=1`
  (`kLogStartPage`), `hi=65536` (sentinel = «чип заполнен»,
  соответствует 16 МБ/256 Б P25Q128H), до ~16 запросов `FLASH_READ`.
  Страница считается свободной только если ВСЕ 256 байт читаются как
  0xFF (в отличие от `flashFindWritePos` на стороне прошивки — там для
  курсора записи читаются только первые 4 байта заголовка записи; два
  разных алгоритма для разных задач, не путать). UI: `QGroupBox
  groupFlashStat`, `QProgressBar barFlashMem` (% занятости), `QLabel
  lblFlashDetail` (первая свободная страница / «чип заполнен» / «нет
  ответа Flash»). Запускается при переключении на вкладку «Тест
  памяти»/«Данные», после стирания чипа/образа, и повторно через 1500 мс
  при таймауте. Закрывает заодно старый TODO «"Прочитать" не видит
  реальные данные устройства, привезённого из поля» (см. «Известные
  ограничения» выше — тот пункт можно считать закрытым тем же
  механизмом, отдельно не обновлялся).
  ~~⚠️ «нет ответа Flash» во время идущего теста циклограммы~~ —
  **разобрано и закрыто 03.07.2026**: две причины, обе устранены.
  (1) 02.07 (старая прошивка) — конкуренция трафика на общей линии,
  поиск загейчен по `m_stendActive` ещё тогда; (2) с прошивкой 02.07+
  (режимы A/B) во время теста регистратор вообще уходит из Service в
  автомат с реальным Stop2 и на LTP-запросы не отвечает ПО ДИЗАЙНУ —
  все периодические опросы 0x8D на время теста подавлены (см.
  «Стендовый сценарий» в разделе «Режимы работы»), поиск запускается
  по завершении циклограммы из stendStop().

- **Индикатор состояния памяти на вкладке «Тест памяти»** (заметка
  03.07.2026, по запросу пользователя, без детальной проработки).
  Внизу вкладки, под рядом кнопок (Запись/Чтение/⚠ Страница/⚠ Сектор/
  ⚠ Чип), сделать «хороший индикатор состояния памяти». Прообраз уже
  есть — панель «Память устройства» на вкладке «Стенд» (barFlashMem +
  Занято/Свободно от двоичного поиска, с 03.07 переехала в правую
  колонку «Стенда»); здесь нужна, вероятно, более подробная версия
  (занятость, режим содержимого RG/LOG от probeFlashImageState,
  диапазон записанного образа, статус ответа Flash). Что именно
  показывать и как визуально — обсудить перед реализацией.

- **Нет места в LOGLSMW для отображения FRAM** (заметка 22.06.2026,
  поднята во время сессии с подключённым реальным железом — FRAM сейчас
  физически в составе стенда/регистратора, см. «Два аппаратных варианта»
  выше про шину FRAM SPI1/SPI3). Сейчас ни одна вкладка LOGLSMW не
  показывает ни состояние, ни содержимое FRAM (TMP117 имеет индикатор,
  Flash — вкладки «Тест памяти»/«Данные», у FRAM — ничего). Не определено:
  что именно нужно показывать (статус подключения/идентификатор чипа,
  гекс-дамп как у Flash, какие-то конкретные поля), и нужна ли отдельная
  вкладка или достаточно ячейки в существующей панели. Требует отдельного
  обсуждения перед реализацией — зафиксировано здесь, чтобы не потерялось
  между сессиями.

---

## Инструменты

- **Прошивка**: STM32CubeIDE + CMake + arm-none-eabi-gcc
- **Qt приложение**: Qt5, qmake, Qt Creator (`SoftWare/CtrlRVZD/`)
- **Программатор**: ST-Link через XP1 (SWD: PA13/PA14)
- **Сервисный порт**: XP2 — USART2, **921600 бод** (вариант A); BLE → USART1, 115200 бод
- **Генератор ТЗ**: `Scripts/build_tz.js` → `md/LogLSM_TZ_v4_consolidated.docx`
