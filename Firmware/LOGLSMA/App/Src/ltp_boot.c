/* ---------------------------------------------------------------------------
 * ltp_boot.c — копия LTP для загрузчика. Смысл и обоснование — в ltp_boot.h.
 *
 * Файл сознательно состоит из включения ltp.c: исходник протокола обязан быть
 * ОДИН. Правки в ltp.c автоматически попадают в обе копии.
 *
 * ⚠ Весь этот объектный файл линкер кладёт в .bootsec (см. STM32L433XX_FLASH.ld).
 * ------------------------------------------------------------------------- */
#include "ltp_boot.h"
#include "../Src/ltp.c"
