# Handoff 20.07.2026 — откат активации, точка продолжения

> **ЧИТАТЬ ПЕРВЫМ в следующей сессии.** Сессия 19–20.07 почти вся ушла на откат.
> Итоговое рабочее состояние: **A = 26.07.19 18:58, W = 26.07.19 20:08.**

## Текущее состояние (ПОДТВЕРЖДЕНО НА ЖЕЛЕЗЕ, всё работает)
- Прошито/собрано: **A = 18:58**, **W = 20:08** — это исходные версии ДО правок
  активации. «Данные» рисуют все 4 графика (500-цикловый образ + живые данные),
  наработка/скорость/температура/питание — ОК.
- **Детектор полок скорости РАБОТАЕТ** (проверено в режиме старт/стоп: полки
  220/164/191/246◄, максимум зелёным). Живёт в: Data.c §189 (полоса
  ±max(FRAC·avg,MIN), дебаунс, subMax[]), com.c §565 (суб-пуш 0x29, только
  сервис/тест, в Flash не идёт), W `stendFillSubSpeeds` + вывод «задано→измерено».

## Что произошло в этой сессии (ВАЖНО не повторить)
1. Реализовал активацию **Шаг 1** (метка ts_activation в внутр. Flash стр.123,
   LTP 0x27/0x28) и **Шаг 2** (паспорт: серийник/вариант/дата стр.123, 0x2A,
   гейт активации по серийнику; цвета шкалы жёлтый/серый).
2. После этого **«Данные» сломались**: рисовалось только «активное время»,
   мелькало и исчезало (гироскоп/акселерометр пусто). Точную мою правку-виновника
   локализовать не удалось (тракт архива→«Данные» я формально не трогал), но
   **откат ВСЕХ моих правок починил** → значит регресс был мой (W-сторона).
3. **ВСЁ ОТКАЧЕНО ВРУЧНУЮ** (точечно, тем же Edit; git тут НЕ использовать —
   один «Initial commit», diff показывает почти весь файл изменённым = расхождение
   переносов строк/путей песочницы; слепой `git checkout` УНИЧТОЖИЛ БЫ
   незакоммиченную работу). Откачены: iflash.c/.h, com.c, Data.c/.h; W —
   mainwindow.cpp/.h, devicecontroller.h, activationbar.cpp/.h, main.cpp; docs.
   Проверено grep: ни одной висячей ссылки на добавленные символы.

## ⚠⚠ КРИТИЧЕСКОЕ ПРЕДУПРЕЖДЕНИЕ (причина потери данных — НЕ мой код активации)
**Кнопка ▶ «полная активация»** (btnActivate → `activationStart()` → старая
машина `ActStep`) ДЕСТРУКТИВНА: шаг `ActStep::Erase` = **ПОЛНОЕ стирание чипа**
(FLASH_ERASE), шаг `ActStep::TestWrite` пишет 0x55 в стр.0 → **УНИЧТОЖАЕТ все
записи циклов на внешней NOR**. В этой сессии это стёрло реальные рабочие данные
(chip erase необратим). **ПЕРЕД любым возвратом к активации:**
- НЕ запускать ▶ на приборе с данными;
- по плану §11 — перепривязать ▶ на НЕдеструктивную цепочку сегментов, и/или
  сделать **авто-дамп Flash на диск ДО любой деструктивной операции**.

## Точка продолжения (план §11 CLAUDE.md, делать ПО ОДНОМУ шагу + проверять «Данные» после каждого)
1. **Активация Шаг 1** заново, аккуратно: ts_activation в внутр. Flash стр.123
   (magic|ts|ready|CRC16, RMW, счётчики стр.124-127 не трогать), LTP 0x27
   GET_PASSPORT / 0x28 SET_ACTIVATION (пишет текущее RTC, база 2000), GET_STATS →
   реальный ts_activation, сегмент «Активация» + честная надпись.
   **После правки W — сразу проверить, что «Данные» (образ 500 циклов) рисуются!**
2. **Шаг 2**: паспорт серийник/вариант/дата (стр.123 хвостом, 0x2A SET_PASSPORT),
   гейт активации по серийнику. UI-поля уже есть (editSerial/cmbDevVariant/
   editProdDate/btnDevInfoRead/btnDevInfoWrite — НЕ подключены).
3. Потом: два пароля (обычный Сервис + «ШЕФ» на «Калибровке», сброса ID нет),
   авто-дамп Flash при 1-м подключении активированного + режим «показ из файла»,
   перепривязка ▶ на недеструктивную цепочку, конфиг+калибровки в стр.123.

## Правила от пользователя (соблюдать)
- Команда на прошивку — **указывать версию И A, И W**.
- Выводить **минимум текста**; спросит — уточнит сам.
- Работать **по одному шагу с проверкой на каждом**, без «пакетных» больших правок.

## Справка по коду (для быстрого старта)
- Прошивка A: Firmware/LOGLSMA/App/{Src,Inc}. Версия = 5 `#define FW_YY..MI` в
  com.c. Внутр. Flash — App/Src/iflash.c (журнал стр.124-127; стр.123 = паспорт,
  СВОБОДНА). Таблица команд `CMD[128]` в com.c (сейчас последняя = 0x26 cmdWdgTest).
- W: SoftWare/LOGLSMW. Версия = `LOGLSMW_VERSION` в main.cpp. LtpCmd — в
  devicecontroller.h (последняя занятая 0x26 WDG_TEST, 0x29 SUBSPEED_PUSH исходящий;
  0x27/0x28/0x2A СВОБОДНЫ). Разбор ответов — onResponse switch(cmd) в mainwindow.cpp.
  Тракт «Данные»: archiveRescanFull→archiveParseRegistratorPage→archiveFinish→
  archiveUpdateDashboard (НЕ ломать — гироскоп/accel рисуются только при
  m_arc.mode==Registrator).
